<?xml version='1.0' encoding='UTF-8'?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" version="2.0"><channel><title>Forestlake</title><link>https://doulifv.github.io/blog</link><description>👨🏻‍💻一个业余玩家，没事就喜欢玩一些网络技术。</description><copyright>Forestlake</copyright><docs>http://www.rssboard.org/rss-specification</docs><generator>python-feedgen</generator><image><url>https://i.mji.rip/2025/10/10/233dd815c949e025f1251a319cb22724.jpeg</url><title>avatar</title><link>https://doulifv.github.io/blog</link></image><lastBuildDate>Fri, 24 Apr 2026 14:51:14 +0000</lastBuildDate><managingEditor>Forestlake</managingEditor><ttl>60</ttl><webMaster>Forestlake</webMaster><item><title>基于Python编写的PPT倒计时软件 支持Windows7 32位</title><link>https://doulifv.github.io/blog/post/ji-yu-Python-bian-xie-de-PPT-dao-ji-shi-ruan-jian-%20-zhi-chi-Windows7%2032-wei.html</link><description>```
import tkinter as tk
from tkinter import colorchooser, simpledialog
import win32com.client
import pythoncom
from comtypes import COMError
import json
import os

# --- 配置文件管理 ---
CONFIG_FILE = 'ppt_timer_config.json'

class ConfigManager:
    def __init__(self):
        self.default_config = {
            'x': 200,
            'y': 200,
            'font_size': 48,
            'font_color': '#FF0000',
            'bg_color': '#000000',
            'alpha': 0.85,
            'duration_sec': 300
        }
        self.config = self.load()

    def load(self):
        if os.path.exists(CONFIG_FILE):
            try:
                with open(CONFIG_FILE, 'r', encoding='utf-8') as f:
                    data = json.load(f)
                    # 合并配置，防止旧配置文件缺少新字段
                    return {**self.default_config, **data}
            except Exception as e:
                print(f'Config load error: {e}')
                pass
        return self.default_config.copy()

    def save(self):
        try:
            with open(CONFIG_FILE, 'w', encoding='utf-8') as f:
                json.dump(self.config, f, indent=4)
        except Exception as e:
            print(f'Config save error: {e}')

# --- 主程序 ---
class PPTAutoTimer:
    def __init__(self, root):
        self.root = root
        self.root.title('PPT Timer')
        self.cfg = ConfigManager()

        # --- 状态变量 ---
        self.time_left = self.cfg.config['duration_sec']
        self.is_running = False
        self.is_ppt_playing = False
        self.is_overtime = False
        self.overtime_seconds = 0
        
        # --- COM 对象缓存 ---
        self.ppt_app = None

        # --- UI 初始化 ---
        self.setup_window()
        self.create_widgets()
        
        # 初始调整窗口大小
        self.root.after(100, self.adjust_window_size)

        # --- 启动状态监测 (使用 after 代替 threading) ---
        self.check_ppt_status()

    def setup_window(self):
        self.root.overrideredirect(True)
        self.root.attributes('-topmost', True)
        self.root.attributes('-alpha', self.cfg.config['alpha'])
        self.root.configure(bg=self.cfg.config['bg_color'])

        # 尝试设置初始位置
        try:
            self.root.geometry(f'+{self.cfg.config['x']}+{self.cfg.config['y']}')
        except:
            pass

        self.root.bind('&lt;ButtonPress-1&gt;', self.start_move)
        self.root.bind('&lt;B1-Motion&gt;', self.do_move)
        self.root.bind('&lt;Button-3&gt;', self.show_menu)

    def create_widgets(self):
        self.label = tk.Label(
            self.root,
            text='00:00',
            font=('Microsoft YaHei', self.cfg.config['font_size'], 'bold'),
            fg=self.cfg.config['font_color'],
            bg=self.cfg.config['bg_color'],
            bd=0,
            padx=0,
            pady=0
        )
        self.label.pack()

        self.menu = tk.Menu(self.root, tearoff=0)
        self.menu.add_command(label='设置时长 (分:秒)', command=self.set_duration)
        self.menu.add_command(label='设置字体大小', command=self.set_font_size)
        self.menu.add_command(label='设置字体颜色', command=self.pick_font_color)
        self.menu.add_command(label='设置背景颜色', command=self.pick_bg_color)
        self.menu.add_command(label='设置透明度', command=self.set_opacity)
        
        self.menu.add_separator()
        self.menu.add_command(label='退出', command=self.exit_app)

    def exit_app(self):
        # 保存当前窗口位置
        try:
            self.cfg.config['x'] = self.root.winfo_x()
            self.cfg.config['y'] = self.root.winfo_y()
            self.cfg.save()
        except:
            pass
        self.root.destroy()

    def set_opacity(self):
        current_percent = int(self.cfg.config['alpha'] * 100)
        val = simpledialog.askinteger('设置透明度', '请输入不透明度百分比 (10-100):', initialvalue=current_percent, minvalue=10, maxvalue=100, parent=self.root)
        if val is not None:
            alpha_value = val / 100.0
            self.cfg.config['alpha'] = alpha_value
            self.cfg.save()
            self.root.attributes('-alpha', alpha_value)

    # --- 核心逻辑：监测 PPT (优化版：单线程轮询) ---
    def check_ppt_status(self):
        is_playing_now = False
        
        try:
            # 1. 尝试获取或复用 PPT 对象
            if self.ppt_app is None:
                self.ppt_app = win32com.client.GetActiveObject('PowerPoint.Application')
            
            # 2. 检查放映窗口数量
            # 注意：这里需要确保 Count 属性存在，防止极罕见的竞态条件
            if self.ppt_app.SlideShowWindows and self.ppt_app.SlideShowWindows.Count &gt; 0:
                is_playing_now = True
                
        except (COMError, pythoncom.com_error, AttributeError):
            # 如果 PPT 没开，或者 COM 连接断开，重置对象引用
            self.ppt_app = None
            is_playing_now = False
        except Exception:
            # 捕获其他未知错误，防止程序崩溃
            is_playing_now = False

        # --- 状态机逻辑 ---
        if is_playing_now and not self.is_ppt_playing:
            # 状态：从 '没播放' 变为 '正在播放' -&gt; 启动计时
            self.is_ppt_playing = True
            self.start_timer()
        elif not is_playing_now and self.is_ppt_playing:
            # 状态：从 '正在播放' 变为 '没播放' -&gt; 停止/重置
            self.is_ppt_playing = False
            self.reset_timer()

        # 无论状态如何，1秒后再次检查 (1000ms)
        self.root.after(1000, self.check_ppt_status)

    def start_timer(self):
        self.is_running = True
        self.is_overtime = False
        self.overtime_seconds = 0
        self.time_left = self.cfg.config['duration_sec']
        self.update_display()
        self.run_timer_step()

    def run_timer_step(self):
        if self.is_running:
            # 只有当 PPT 还在播放时才继续计时
            if self.is_ppt_playing:
                if self.time_left &gt; 0:
                    # --- 倒计时阶段 ---
                    self.time_left -= 1
                    self.is_overtime = False
                else:
                    # --- 超时阶段 ---
                    self.is_overtime = True
                    self.overtime_seconds += 1

                self.update_display()
                # 1秒后继续下一步
                self.root.after(1000, self.run_timer_step)
            else:
                # 如果计时器还在运行但 PPT 停止了（通过菜单强制停止等情况）
                pass

    def reset_timer(self):
        self.is_running = False
        self.overtime_seconds = 0
        # 这里的 update_display 是为了让界面立即变回 00:00 或初始状态
        # 如果希望 PPT 退出后保留最后的时间，可以去掉下面这行
        self.update_display()

    def update_display(self):
        if self.is_overtime:
            # --- 超时显示逻辑 ---
            mins, secs = divmod(self.overtime_seconds, 60)
            time_str = f'+{mins:02d}:{secs:02d}'
        else:
            # --- 倒计时显示逻辑 ---
            mins, secs = divmod(self.time_left, 60)
            time_str = f'{mins:02d}:{secs:02d}'

        self.label.config(text=time_str, fg=self.cfg.config['font_color'])
        self.adjust_window_size()

    def adjust_window_size(self):
        try:
            self.label.update_idletasks()
            text_width = self.label.winfo_reqwidth()
            text_height = self.label.winfo_reqheight()

            padding_x = 20
            padding_y = 10

            final_width = text_width + padding_x
            final_height = text_height + padding_y

            # 获取当前坐标以保持位置不变
            x = self.root.winfo_x()
            y = self.root.winfo_y()

            final_width = max(final_width, 50)
            final_height = max(final_height, 30)

            self.root.geometry(f'{final_width}x{final_height}+{x}+{y}')
        except:
            pass

    # --- 交互功能 ---
    def set_duration(self):
        input_str = simpledialog.askstring('设置时长', '请输入倒计时时长 (支持格式 MM:SS 或纯秒数):', initialvalue=str(self.cfg.config['duration_sec']), parent=self.root)
        if input_str:
            try:
                input_str = input_str.replace('：', ':')
                if ':' in input_str:
                    m, s = map(int, input_str.split(':'))
                    total_seconds = m * 60 + s
                else:
                    total_seconds = int(input_str)

                if total_seconds &gt; 0:
                    self.cfg.config['duration_sec'] = total_seconds
                    self.cfg.save()
                    # 如果当前没有在运行，立即更新显示
                    if not self.is_running:
                        self.time_left = total_seconds
                        self.is_overtime = False
                        self.overtime_seconds = 0
                        self.update_display()
            except ValueError:
                pass

    def set_font_size(self):
        val = simpledialog.askinteger('字体大小', '输入字体大小 (像素):', initialvalue=self.cfg.config['font_size'], minvalue=10, maxvalue=200, parent=self.root)
        if val:
            self.cfg.config['font_size'] = val
            self.label.config(font=('Microsoft YaHei', val, 'bold'))
            self.cfg.save()
            self.adjust_window_size()

    def pick_font_color(self):
        color = colorchooser.askcolor(color=self.cfg.config['font_color'], parent=self.root)[1]
        if color:
            self.cfg.config['font_color'] = color
            self.label.config(fg=color)
            self.cfg.save()

    def pick_bg_color(self):
        color = colorchooser.askcolor(color=self.cfg.config['bg_color'], parent=self.root)[1]
        if color:
            self.cfg.config['bg_color'] = color
            self.root.configure(bg=color)
            self.label.config(bg=color)
            self.cfg.save()

    def show_menu(self, event):
        self.menu.tk_popup(event.x_root, event.y_root)

    def start_move(self, event):
        self.x = event.x
        self.y = event.y

    def do_move(self, event):
        deltax = event.x - self.x
        deltay = event.y - self.y
        x = self.root.winfo_x() + deltax
        y = self.root.winfo_y() + deltay
        self.root.geometry(f'+{x}+{y}')

if __name__ == '__main__':
    # 尝试处理 DPI 设置 (兼容 Win7/10/11)
    try:
        from ctypes import windll
        # SetProcessDpiAwareness(1) -&gt; SYSTEM_DPI_AWARE
        windll.shcore.SetProcessDpiAwareness(1)
    except Exception:
        # Win7 可能没有 shcore 或者权限不足，忽略错误
        pass

    root = tk.Tk()
    app = PPTAutoTimer(root)
    root.mainloop()
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/ji-yu-Python-bian-xie-de-PPT-dao-ji-shi-ruan-jian-%20-zhi-chi-Windows7%2032-wei.html</guid><pubDate>Fri, 24 Apr 2026 14:50:46 +0000</pubDate></item><item><title>Easytier虚拟组网</title><link>https://doulifv.github.io/blog/post/Easytier-xu-ni-zu-wang.html</link><description>https://easytier.cn/guide/installation.html
**一键安装脚本（仅 Linux）**
```
wget -O /tmp/easytier.sh 'https://raw.githubusercontent.com/EasyTier/EasyTier/main/script/install.sh' &amp;&amp; sudo bash /tmp/easytier.sh install --gh-proxy https://ghfast.top/
```
&gt;Install EasyTier successfully!
&gt;Default Port: 11010(UDP+TCP), Notice allowing in firewall!
&gt;Default Network Name: default, Please change it to your own network name!
&gt;Now EasyTier supports multiple config files. You can create config files in the /opt/easytier/config/ folder
&gt;For more information, please check the documents in official site
&gt;The management example of a single configuration file is as follows
&gt;Status: systemctl status easytier@default
&gt;Start: systemctl start easytier@default
&gt;Restart: systemctl restart easytier@default
&gt;Stop: systemctl stop easytier@default
---
stop easytier@default
```
systemctl stop easytier@default
```
Run in private-mode 
```
easytier-core --private-mode true --network-name {my-network} --network-secret {my-secret}
```
运行后程序会print 配置文件，尝试把它复制到/opt/easytier/config文件夹并且把它运行起来
&gt;instance_name = 'default'
dhcp = true
listeners = [
    'tcp://0.0.0.0:11010',
    'udp://0.0.0.0:11010',
    'wg://0.0.0.0:11011',
    'ws://0.0.0.0:11011/',
    'wss://0.0.0.0:11012/',
]
exit_nodes = []
rpc_portal = '0.0.0.0:0'
[[peer]]
uri = 'tcp://public.easytier.top:11010'
[network_identity]
network_name = 'default'
network_secret = 'default'
[flags]
default_protocol = 'udp'
dev_name = ''
enable_encryption = true
enable_ipv6 = true
mtu = 1380
latency_first = false
enable_exit_node = false
no_tun = false
use_smoltcp = false
foreign_network_whitelist = '*'
disable_p2p = false
p2p_only = false
relay_all_peer_rpc = false
disable_tcp_hole_punching = false
disable_udp_hole_punching = false

客户端配置[测试运行/临时运行]
```
easytier-core  -i 10.126.126.2   -p udp://服务器IP地址:11010 --network-name {my-network} --network-secret {my-secret} --hostname my-nas
```
&gt; -i 用户手动指定IP地址 也可以为 -d 机器自动通过DHCP获取IP地址 
&gt; -p 是服务器地址包括前面的qiuc://也是由用户输入 
&gt; --network-name 是网络名称 用户输入 （可空）
&gt; --network-secret 是网络密码 （可空）
&gt; --hostname 主机名称 （可空）
&gt; -n 子网代理

客户端安装为服务
```
easytier-cli service install  -i 10.126.126.2   -p udp://服务器IP地址:11010 --network-name {my-network} --network-secret {my-secret} --hostname my-nas
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Easytier-xu-ni-zu-wang.html</guid><pubDate>Fri, 24 Apr 2026 14:43:19 +0000</pubDate></item><item><title>windows静态路由操作</title><link>https://doulifv.github.io/blog/post/windows-jing-tai-lu-you-cao-zuo.html</link><description>临时路由
```
route add &lt;目标网络&gt; mask &lt;子网掩码&gt; &lt;网关地址&gt;
```
永久路由-[跃点可缺省]
```
route -p add &lt;目标网络&gt; mask &lt;子网掩码&gt; &lt;网关地址&gt; metric &lt;跃点数&gt;
```
路由表查看
```
route print 
```
测试路由
```
tracert &lt;IP地址&gt; 
```
静态路由删除
```
route delete 
```
改变路由
```
route change &lt;目标网络&gt; mask &lt;子网掩码&gt; &lt;新网关&gt; metric &lt;新跃点数&gt; 
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/windows-jing-tai-lu-you-cao-zuo.html</guid><pubDate>Tue, 31 Mar 2026 07:17:00 +0000</pubDate></item><item><title>晚饭吃什么？ [dinner-lottery]给你意见</title><link>https://doulifv.github.io/blog/post/wan-fan-chi-shen-me-%EF%BC%9F%20%5Bdinner-lottery%5D-gei-ni-yi-jian.html</link><description>开源地址 [dinner-lottery](https://github.com/doulifv/dinner-lottery)
在线体验 [dinner](https://dinner.4566.link)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/wan-fan-chi-shen-me-%EF%BC%9F%20%5Bdinner-lottery%5D-gei-ni-yi-jian.html</guid><pubDate>Fri, 06 Feb 2026 15:01:19 +0000</pubDate></item><item><title>Debian11换源</title><link>https://doulifv.github.io/blog/post/Debian11-huan-yuan.html</link><description>Debian Buster 以上版本默认支持 HTTPS 源。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Debian11-huan-yuan.html</guid><pubDate>Fri, 19 Dec 2025 00:06:50 +0000</pubDate></item><item><title>alpine下docker换源操作</title><link>https://doulifv.github.io/blog/post/alpine-xia-docker-huan-yuan-cao-zuo.html</link><description>创建/修改daemon.json文件
```
vi /etc/docker/daemon.json
```
编辑
```
{
  'registry-mirrors': ['https://mirror.ccs.tencentyun.com', 'https://hub-mirror.c.163.com']
}
```
重启验证
```
rc-service docker stop
rc-service docker start
docker info
docker pull hello-world
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/alpine-xia-docker-huan-yuan-cao-zuo.html</guid><pubDate>Mon, 15 Dec 2025 13:23:28 +0000</pubDate></item><item><title>alpine安装docker</title><link>https://doulifv.github.io/blog/post/alpine-an-zhuang-docker.html</link><description>```
vi /etc/apk/repositories
```
&gt;#http://mirrors.aliyun.com/alpine/v3.21/community
&gt;取消community源的注释
```
apk update
apk add docker
```
添加开机自启动、启动docker服务
```
rc-update add docker boot
service docker start
```
&gt; alpine:~# rc-update add docker boot
&gt;   * service docker added to runlevel boot
&gt;alpine:~# service docker start
&gt;   * Caching service dependencies ...                                                         [ ok ]
&gt;   * /var/log/docker.log: creating file
&gt;   * /var/log/docker.log: correcting owner
&gt;   * Starting Docker Daemon ...                                                               [ ok  ]
&gt; alpine:~# docker -v
&gt;Docker version 27.3.1。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/alpine-an-zhuang-docker.html</guid><pubDate>Mon, 15 Dec 2025 03:32:01 +0000</pubDate></item><item><title>linux开启流量转发</title><link>https://doulifv.github.io/blog/post/linux-kai-qi-liu-liang-zhuan-fa.html</link><description>### 启用 IP 转发 (Kernel Level)
#### 1.编辑 sysctl 配置文件
```
vi /etc/sysctl.conf
```
#### 2.取消注释或添加以下行

- 对于 IPv4：
```
net.ipv4.ip_forward=1
```
- 对于 IPv6 (如果需要)：
```
net.ipv6.conf.all.forwarding=1
```
#### 3.应用更改
```
sudo sysctl -p
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/linux-kai-qi-liu-liang-zhuan-fa.html</guid><pubDate>Sat, 13 Dec 2025 03:30:28 +0000</pubDate></item><item><title>windows10TLSC开启图片查看器功能</title><link>https://doulifv.github.io/blog/post/windows10TLSC-kai-qi-tu-pian-cha-kan-qi-gong-neng.html</link><description>将以下复制保存为PhotoViewer.reg
```
Windows Registry Editor Version 5.00

[HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows Photo Viewer\Capabilities\FileAssociations]
'.jpg'='PhotoViewer.FileAssoc.Tiff'
'.jpeg'='PhotoViewer.FileAssoc.Tiff'
'.gif'='PhotoViewer.FileAssoc.Tiff'
'.png'='PhotoViewer.FileAssoc.Tiff'
'.bmp'='PhotoViewer.FileAssoc.Tiff'
'.tiff'='PhotoViewer.FileAssoc.Tiff'
'.ico'='PhotoViewer.FileAssoc.Tiff'
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/windows10TLSC-kai-qi-tu-pian-cha-kan-qi-gong-neng.html</guid><pubDate>Wed, 10 Dec 2025 06:59:45 +0000</pubDate></item><item><title>NTP同步软件</title><link>https://doulifv.github.io/blog/post/NTP-tong-bu-ruan-jian.html</link><description>使用Python编写，适合windows7 32位及以上系统，整体功能纯净无广告
软件名称：NTP同步
图形界面（tkinter + 系统托盘）
自定义 NTP 服务器 &amp; 同步间隔
无黑框（使用 pythonw 或打包后自动隐藏）
圆形蓝色托盘图标（带时钟指针）
开机启动开关按钮
通过 Windows API 设置时间
以管理员身份自动提权
兼容 Windows 7 32位 + Python 3.8

编译所需要的依赖
```
pip install ntplib pystray pillow
```
打包成软件
```
pyinstaller --onefile --windowed --name 'NTP同步' ntp_gui.py
```
⚠️ 重要提醒

- 首次运行会弹出 UAC 提权窗口（正常现象）

- 若仍提示“设置系统时间失败”，请手动授予当前用户 “更改系统时间”权限（通过 secpol.msc）
- 开机启动功能写入 当前用户 启动项，无需管理员权限

---
ntp_gui.py
```
# ntp_gui.py - NTP同步（Windows 7 32位兼容版）
# 功能：图形界面 + 系统托盘 + 开机启动 + 无黑框 + 权限提取

import os
import sys
import time
import threading
import subprocess
from datetime import datetime, timezone
import tkinter as tk
from tkinter import ttk, messagebox
import ntplib
import pystray
from PIL import Image, ImageDraw

# Windows 注册表（仅 Windows）
try:
    import winreg as reg
except ImportError:
    reg = None

# ----------------------------
# 工具函数
# ----------------------------

def is_admin():
    try:
        return os.getuid() == 0
    except AttributeError:
        import ctypes
        return ctypes.windll.shell32.IsUserAnAdmin() != 0

def run_as_admin():
    '''以管理员身份重新运行（优先使用 pythonw 避免黑框）'''
    import ctypes
    executable = sys.executable
    if executable.endswith('python.exe'):
        executable = executable[:-10] + 'pythonw.exe'
    script = os.path.abspath(sys.argv[0])
    params = f''{script}''
    try:
        ctypes.windll.shell32.ShellExecuteW(None, 'runas', executable, params, None, 1)
    except Exception:
        pass
    sys.exit(0)

def set_system_time(dt):
    '''使用 Windows API 设置系统时间（支持 Win7 32位）'''
    import ctypes
    from ctypes import wintypes

    class LUID(ctypes.Structure):
        _fields_ = [('LowPart', wintypes.DWORD), ('HighPart', wintypes.LONG)]

    class LUID_AND_ATTRIBUTES(ctypes.Structure):
        _fields_ = [('Luid', LUID), ('Attributes', wintypes.DWORD)]

    class TOKEN_PRIVILEGES(ctypes.Structure):
        _fields_ = [('PrivilegeCount', wintypes.DWORD), ('Privileges', LUID_AND_ATTRIBUTES * 1)]

    def enable_privilege(privilege_name):
        ADVAPI32 = ctypes.windll.advapi32
        KERNEL32 = ctypes.windll.kernel32
        hToken = wintypes.HANDLE()
        if not ADVAPI32.OpenProcessToken(KERNEL32.GetCurrentProcess(), 0x0028, ctypes.byref(hToken)):
            return False
        luid = LUID()
        if not ADVAPI32.LookupPrivilegeValueW(None, privilege_name, ctypes.byref(luid)):
            KERNEL32.CloseHandle(hToken)
            return False
        tp = TOKEN_PRIVILEGES()
        tp.PrivilegeCount = 1
        tp.Privileges[0].Luid = luid
        tp.Privileges[0].Attributes = 2  # SE_PRIVILEGE_ENABLED
        result = ADVAPI32.AdjustTokenPrivileges(hToken, False, ctypes.byref(tp), 0, None, None)
        success = (result != 0) and (KERNEL32.GetLastError() == 0)
        KERNEL32.CloseHandle(hToken)
        return success

    if not enable_privilege('SeSystemtimePrivilege'):
        return False

    class SYSTEMTIME(ctypes.Structure):
        _fields_ = [
            ('wYear', wintypes.WORD),
            ('wMonth', wintypes.WORD),
            ('wDayOfWeek', wintypes.WORD),
            ('wDay', wintypes.WORD),
            ('wHour', wintypes.WORD),
            ('wMinute', wintypes.WORD),
            ('wSecond', wintypes.WORD),
            ('wMilliseconds', wintypes.WORD),
        ]

    st = SYSTEMTIME()
    st.wYear = dt.year
    st.wMonth = dt.month
    st.wDay = dt.day
    st.wHour = dt.hour
    st.wMinute = dt.minute
    st.wSecond = dt.second
    st.wMilliseconds = 0

    return bool(ctypes.windll.kernel32.SetLocalTime(ctypes.byref(st)))

def get_ntp_time(ntp_server):
    client = ntplib.NTPClient()
    try:
        response = client.request(ntp_server, version=3, timeout=5)
        utc_dt = datetime.utcfromtimestamp(response.tx_time).replace(tzinfo=timezone.utc)
        return utc_dt
    except Exception:
        return None

# ----------------------------
# 主应用类
# ----------------------------

class NTPApp:
    def __init__(self, root):
        self.root = root
        self.root.title('NTP同步')
        self.root.geometry('420x260')
        self.root.resizable(False, False)
        self.root.protocol('WM_DELETE_WINDOW', self.hide_to_tray)

        self.ntp_server = 'pool.ntp.org'
        self.interval = 3600
        self.running = False
        self.sync_thread = None
        self.tray_icon = None

        self.create_widgets()
        self.create_tray_icon()

        # 初始化开机启动状态
        if reg:
            self.autostart_var.set(self.is_autostart_enabled())

    def create_widgets(self):
        pad = {'padx': 20, 'pady': 5}

        ttk.Label(self.root, text='NTP 服务器地址:').pack(**pad, anchor='w')
        self.server_var = tk.StringVar(value=self.ntp_server)
        ttk.Entry(self.root, textvariable=self.server_var, width=45).pack(**pad)

        ttk.Label(self.root, text='同步间隔（秒，≥10）:').pack(**pad, anchor='w')
        self.interval_var = tk.StringVar(value=str(self.interval))
        ttk.Entry(self.root, textvariable=self.interval_var, width=20).pack(**pad, anchor='w')

        btn_frame = ttk.Frame(self.root)
        btn_frame.pack(pady=8)

        self.sync_btn = ttk.Button(btn_frame, text='立即同步', command=self.manual_sync)
        self.sync_btn.pack(side='left', padx=5)

        self.start_stop_btn = ttk.Button(btn_frame, text='开始自动同步', command=self.toggle_sync)
        self.start_stop_btn.pack(side='left', padx=5)

        self.quit_btn = ttk.Button(btn_frame, text='退出', command=self.quit_app)
        self.quit_btn.pack(side='left', padx=5)

        # 开机启动复选框
        if reg:
            self.autostart_var = tk.BooleanVar()
            self.autostart_check = ttk.Checkbutton(
                self.root,
                text='开机自动启动',
                variable=self.autostart_var,
                command=self.toggle_autostart
            )
            self.autostart_check.pack(**pad, anchor='w')
        else:
            ttk.Label(self.root, text='（开机启动仅支持 Windows）', foreground='gray').pack(**pad, anchor='w')

        self.status_var = tk.StringVar(value='就绪')
        ttk.Label(self.root, textvariable=self.status_var, foreground='gray').pack(pady=5)

    def manual_sync(self):
        self._do_sync()

    def _do_sync(self):
        server = self.server_var.get().strip()
        if not server:
            messagebox.showwarning('输入错误', '请输入 NTP 服务器地址！')
            return

        self.status_var.set('正在同步...')
        self.root.update_idletasks()

        utc_time = get_ntp_time(server)
        if utc_time:
            local_time = utc_time.astimezone().replace(tzinfo=None)
            if set_system_time(local_time):
                self.status_var.set(f'✅ 同步成功: {local_time.strftime('%Y-%m-%d %H:%M:%S')}')
            else:
                self.status_var.set('❌ 设置系统时间失败（请检查权限）')
        else:
            self.status_var.set('❌ 无法连接 NTP 服务器')

    def toggle_sync(self):
        if not self.running:
            try:
                interval = int(self.interval_var.get())
                if interval &lt; 10:
                    messagebox.showwarning('警告', '间隔不能小于10秒')
                    return
                self.interval = interval
                self.ntp_server = self.server_var.get().strip()
                if not self.ntp_server:
                    messagebox.showwarning('警告', '请输入 NTP 服务器地址')
                    return
                self.running = True
                self.start_stop_btn.config(text='停止同步')
                self.sync_thread = threading.Thread(target=self._sync_loop, daemon=True)
                self.sync_thread.start()
                self.status_var.set('自动同步已启动')
            except ValueError:
                messagebox.showerror('错误', '请输入有效的数字作为间隔时间')
        else:
            self.running = False
            self.start_stop_btn.config(text='开始自动同步')
            self.status_var.set('自动同步已停止')

    def _sync_loop(self):
        while self.running:
            self._do_sync()
            for _ in range(self.interval):
                if not self.running:
                    break
                time.sleep(1)

    def hide_to_tray(self):
        self.root.withdraw()

    def show_window(self):
        self.root.deiconify()

    def quit_app(self):
        self.running = False
        if self.tray_icon:
            self.tray_icon.stop()
        self.root.destroy()

    # ----------------------------
    # 系统托盘
    # ----------------------------

    def create_image(self):
        size = 64
        image = Image.new('RGBA', (size, size), (0, 0, 0, 0))
        draw = ImageDraw.Draw(image)
        radius = size // 2 - 4
        center = size // 2
        draw.ellipse((center - radius, center - radius, center + radius, center + radius),
                     fill=(30, 144, 255))  # DodgerBlue
        # 时钟指针
        draw.line([(center, center - radius + 10), (center, center - radius // 2)], fill='white', width=3)
        draw.line([(center + radius // 2, center), (center + radius - 10, center)], fill='white', width=3)
        return image

    def create_tray_icon(self):
        menu = (
            pystray.MenuItem('显示窗口', self.on_tray_show),
            pystray.MenuItem('退出', self.on_tray_quit)
        )
        icon = pystray.Icon('ntp_sync', self.create_image(), 'NTP同步', menu)
        self.tray_icon = icon
        threading.Thread(target=icon.run, daemon=True).start()

    def on_tray_show(self, icon, item):
        self.show_window()

    def on_tray_quit(self, icon, item):
        self.quit_app()

    # ----------------------------
    # 开机启动
    # ----------------------------

    def is_autostart_enabled(self):
        if not reg:
            return False
        try:
            key = reg.OpenKey(reg.HKEY_CURRENT_USER,
                              r'Software\Microsoft\Windows\CurrentVersion\Run',
                              0, reg.KEY_READ)
            try:
                value, _ = reg.QueryValueEx(key, 'NTP同步')
                reg.CloseKey(key)
                return bool(value)
            except FileNotFoundError:
                reg.CloseKey(key)
                return False
        except Exception:
            return False

    def toggle_autostart(self):
        if not reg:
            return
        try:
            key = reg.OpenKey(reg.HKEY_CURRENT_USER,
                              r'Software\Microsoft\Windows\CurrentVersion\Run',
                              0, reg.KEY_SET_VALUE)

            if self.autostart_var.get():
                if getattr(sys, 'frozen', False):
                    exe_path = os.path.abspath(sys.executable)
                else:
                    pythonw = sys.executable.replace('python.exe', 'pythonw.exe')
                    script = os.path.abspath(__file__)
                    exe_path = f''{pythonw}' '{script}''
                reg.SetValueEx(key, 'NTP同步', 0, reg.REG_SZ, exe_path)
            else:
                try:
                    reg.DeleteValue(key, 'NTP同步')
                except FileNotFoundError:
                    pass
            reg.CloseKey(key)
        except Exception as e:
            messagebox.showerror('错误', f'设置开机启动失败:\n{str(e)}')
            self.autostart_var.set(False)

# ----------------------------
# 主程序入口
# ----------------------------

def main():
    if not is_admin():
        run_as_admin()

    root = tk.Tk()
    app = NTPApp(root)
    root.mainloop()

if __name__ == '__main__':
    main()
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/NTP-tong-bu-ruan-jian.html</guid><pubDate>Wed, 10 Dec 2025 06:57:03 +0000</pubDate></item><item><title>Linux安装qbittorrent-nox 5.1.0-2</title><link>https://doulifv.github.io/blog/post/Linux-an-zhuang-qbittorrent-nox%205.1.0-2.html</link><description>## 更新/安装/查看日志
```
sudo apt update
sudo apt install qbittorrent-nox
journalctl -fu qbittorrent-nox
```
&gt;******** Information ********
&gt;Nov 29 23:25:30 localhost qbittorrent-nox[145969]: To control qBittorrent, access the WebUI at: http://&gt;localhost:8000
&gt;Nov 29 23:25:30 localhost qbittorrent-nox[145969]: The WebUI administrator username is: **admin**
&gt;Nov 29 23:25:30 localhost qbittorrent-nox[145969]: The WebUI administrator password was not set. A &gt;temporary password is provided for this session: **HH67hgg6**
&gt;Nov 29 23:25:30 localhost qbittorrent-nox[145969]: You should set your own password in program &gt;preferences.
## 不知道密码?
如果没有见到密码输出，可以手动修改配置文件
```
vi /etc/systemd/system/qbittorrent-nox.service
```
添加WorkingDirectory
```
[Unit]
Description=qBittorrent Command Line Client
After=network.target

[Service]
Type=simple
User=root
Group=root
UMask=007
WorkingDirectory=/root/.config/qBittorrent
ExecStart=/usr/bin/qbittorrent-nox --webui-port=8080
Restart=on-failure

[Install]
WantedBy=multi-user.target
```
编辑配置文件
```
vi ~/.config/qBittorrent/qBittorrent.conf
```
添加以下内容
```
[Preferences]
WebUI\User=admin
WebUI\Password_PBKDF2='@ByteArray(ARQ77eY1NUZaQsuDHbIMCA==:0WMRkYTUWVT9wVvdDtHAjU9b3b7uB8NR1Gur2hmQCvCDpm39Q+PsJRJPaCU51dEiz+dTzh8qbPsL8WkFljQYFQ==)'
```
重载/重启服务
```
systemctl stop qbittorrent-nox
systemctl daemon-reload
systemctl start qbittorrent-nox
```
账户admin 
密码adminadmin

参考
[apt安装最新版qbittorrent-nox 5.0.1，并修改密码](https://www.thsink.com/notes/1842/)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Linux-an-zhuang-qbittorrent-nox%205.1.0-2.html</guid><pubDate>Sun, 30 Nov 2025 13:21:47 +0000</pubDate></item><item><title>stalwart自建邮局</title><link>https://doulifv.github.io/blog/post/stalwart-zi-jian-you-ju.html</link><description>## Install on Linux or MacOS   在Linux或MacOS系统上安装
To install Stalwart on Linux or MacOS, execute the following command in your terminal:
要在 Linux 或 MacOS 上安装 Stalwart，请在终端执行以下命令：
```
curl --proto '=https' --tlsv1.2 -sSf https://get.stalw.art/install.sh -o install.sh
```
Then, execute the installation script as root. The default installation directory is /opt/stalwart. If you want to install Stalwart in a different directory, you can specify the installation directory as an argument:
然后，以root身份执行安装脚本。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/stalwart-zi-jian-you-ju.html</guid><pubDate>Wed, 19 Nov 2025 08:35:02 +0000</pubDate></item><item><title>autounlock.bat</title><link>https://doulifv.github.io/blog/post/autounlock.bat.html</link><description>```
@echo off
setlocal enabledelayedexpansion

:: 设置要监控的目录（当前目录）
set 'watch_dir=H:\auto\file'

:: 设置要执行的程序
set 'target_exe=H:\auto\et.exe'

:: 初始化文件列表
dir /b /a-d '%watch_dir%' &gt; oldfiles.tmp 2&gt;nul

:loop
:: 使用ping命令实现2秒等待（ping 本机5次，每次间隔1秒，共约5秒）
ping -n 5 127.0.0.1 &gt; nul

:: 获取当前文件列表
dir /b /a-d '%watch_dir%' &gt; newfiles.tmp 2&gt;nul

:: 比较文件差异
fc /b oldfiles.tmp newfiles.tmp &gt;nul
if %errorlevel% equ 1 (
    echo 检测到新文件，正在执行 %target_exe% ...
    
    :: 启动程序并发送回车键
    start '' /wait cmd /c echo. | '%target_exe%'
    
    echo 程序执行完成，继续监控...
)

:: 更新文件列表
move /y newfiles.tmp oldfiles.tmp &gt;nul
goto loop
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/autounlock.bat.html</guid><pubDate>Fri, 14 Nov 2025 07:20:16 +0000</pubDate></item><item><title>飞鼠说明书备份</title><link>https://doulifv.github.io/blog/post/fei-shu-shuo-ming-shu-bei-fen.html</link><description>几个月不用飞鼠，使用方法给忘记了，马上去抖音找了下才知道怎么用
&lt;img width='415' height='498' alt='Image' src='https://github.com/user-attachments/assets/c8faa0c8-941c-4019-8127-cbb0c661d423' /&gt;
&lt;img width='28' height='498' alt='Image' src='https://github.com/user-attachments/assets/510d251a-3c91-4e43-b625-346ad8fa8433' /&gt;。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/fei-shu-shuo-ming-shu-bei-fen.html</guid><pubDate>Thu, 13 Nov 2025 14:25:17 +0000</pubDate></item><item><title>一键禁用windows10/11系统自动更新至2099年</title><link>https://doulifv.github.io/blog/post/yi-jian-jin-yong-windows10-11-xi-tong-zi-dong-geng-xin-zhi-2099-nian.html</link><description>### 说明：实现原理
实现这个方法的原理很简单。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/yi-jian-jin-yong-windows10-11-xi-tong-zi-dong-geng-xin-zhi-2099-nian.html</guid><pubDate>Tue, 11 Nov 2025 02:37:58 +0000</pubDate></item><item><title>hysteria设置端口跳跃</title><link>https://doulifv.github.io/blog/post/hysteria-she-zhi-duan-kou-tiao-yue.html</link><description>## 安装 iptables-persistent
```
sudo apt update
sudo apt install iptables-persistent
```
默认情况下iptables规则会在重启后失效，因此通过安装iptables-persistent来持久化配置文件
**在安装过程中，系统会提示你是否要保存当前的 iptables 规则，选择“Yes”。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/hysteria-she-zhi-duan-kou-tiao-yue.html</guid><pubDate>Sun, 09 Nov 2025 02:36:02 +0000</pubDate></item><item><title>alipine上安装hysteria</title><link>https://doulifv.github.io/blog/post/alipine-shang-an-zhuang-hysteria.html</link><description>source [alpine-hysteria2](https://github.com/zrlhk/alpine-hysteria2)
---
一键安装
```
wget -O hy2.sh https://raw.githubusercontent.com/zrlhk/alpine-hysteria2/main/hy2.sh  &amp;&amp; sh hy2.sh
```

_说明:配置文件：**/etc/hysteria/config.yaml**
使用自签名证书，默认端口40443，安全tls，SNI为： bing.com
随系统自启动
看状态 **service hysteria status**
重启 **service hysteria restart**_

---

安装脚本源代码
```
#!/bin/bash

apk add wget curl git openssh openssl openrc

generate_random_password() {
  dd if=/dev/random bs=18 count=1 status=none | base64
}

GENPASS='$(generate_random_password)'

echo_hysteria_config_yaml() {
  cat &lt;&lt; EOF
listen: :40443


#有域名，使用CA证书
#acme:
#  domains:
#    - test.heybro.bid #你的域名，需要先解析到服务器ip
#  email: xxx@gmail.com

#使用自签名证书
tls:
  cert: /etc/hysteria/server.crt
  key: /etc/hysteria/server.key

auth:
  type: password
  password: $GENPASS

masquerade:
  type: proxy
  proxy:
    url: https://bing.com/
    rewriteHost: true
EOF
}

echo_hysteria_autoStart(){
  cat &lt;&lt; EOF
#!/sbin/openrc-run

name='hysteria'

command='/usr/local/bin/hysteria'
command_args='server --config /etc/hysteria/config.yaml'

pidfile='/var/run/${name}.pid'

command_background='yes'

depend() {
        need networking
}

EOF
}


wget -O /usr/local/bin/hysteria https://download.hysteria.network/app/latest/hysteria-linux-amd64  --no-check-certificate
chmod +x /usr/local/bin/hysteria

mkdir -p /etc/hysteria/

openssl req -x509 -nodes -newkey ec:&lt;(openssl ecparam -name prime256v1) -keyout /etc/hysteria/server.key -out /etc/hysteria/server.crt -subj '/CN=bing.com' -days 36500

#写配置文件
echo_hysteria_config_yaml &gt; '/etc/hysteria/config.yaml'

#写自启动
echo_hysteria_autoStart &gt; '/etc/init.d/hysteria'
chmod +x /etc/init.d/hysteria
#启用自启动
rc-update add hysteria

service hysteria start

#启动hy2
#/usr/local/bin/hysteria  server --config /etc/hysteria/config.yaml &amp;

echo '------------------------------------------------------------------------'
echo 'hysteria2已经安装完成'
echo '默认端口： 40443 ， 密码为： $GENPASS ，工具中配置：tls，SNI为： bing.com'
echo '配置文件：/etc/hysteria/config.yaml'
echo '已经随系统自动启动'
echo '看状态 service hysteria status'
echo '重启 service hysteria restart'
echo '请享用。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/alipine-shang-an-zhuang-hysteria.html</guid><pubDate>Sat, 08 Nov 2025 01:03:19 +0000</pubDate></item><item><title>TUIC v5一键脚本</title><link>https://doulifv.github.io/blog/post/TUIC%20v5-yi-jian-jiao-ben.html</link><description>```
#!/bin/bash

export LANG=en_US.UTF-8

RED='\033[31m'
GREEN='\033[32m'
YELLOW='\033[33m'
PLAIN='\033[0m'

red(){
    echo -e '\033[31m\033[01m$1\033[0m'
}

green(){
    echo -e '\033[32m\033[01m$1\033[0m'
}

yellow(){
    echo -e '\033[33m\033[01m$1\033[0m'
}

# 判断系统及定义系统安装依赖方式
REGEX=('debian' 'ubuntu' 'centos|red hat|kernel|oracle linux|alma|rocky' ''amazon linux'' 'fedora')
RELEASE=('Debian' 'Ubuntu' 'CentOS' 'CentOS' 'Fedora')
PACKAGE_UPDATE=('apt-get update' 'apt-get update' 'yum -y update' 'yum -y update' 'yum -y update')
PACKAGE_INSTALL=('apt -y install' 'apt -y install' 'yum -y install' 'yum -y install' 'yum -y install')
PACKAGE_REMOVE=('apt -y remove' 'apt -y remove' 'yum -y remove' 'yum -y remove' 'yum -y remove')
PACKAGE_UNINSTALL=('apt -y autoremove' 'apt -y autoremove' 'yum -y autoremove' 'yum -y autoremove' 'yum -y autoremove')

[[ $EUID -ne 0 ]] &amp;&amp; red '注意: 请在root用户下运行脚本' &amp;&amp; exit 1

CMD=('$(grep -i pretty_name /etc/os-release 2&gt;/dev/null | cut -d \' -f2)' '$(hostnamectl 2&gt;/dev/null | grep -i system | cut -d : -f2)' '$(lsb_release -sd 2&gt;/dev/null)' '$(grep -i description /etc/lsb-release 2&gt;/dev/null | cut -d \' -f2)' '$(grep . /etc/redhat-release 2&gt;/dev/null)' '$(grep . /etc/issue 2&gt;/dev/null | cut -d \\ -f1 | sed '/^[ ]*$/d')')

for i in '${CMD[@]}'; do
    SYS='$i' &amp;&amp; [[ -n $SYS ]] &amp;&amp; break
done

for ((int = 0; int &lt; ${#REGEX[@]}; int++)); do
    [[ $(echo '$SYS' | tr '[:upper:]' '[:lower:]') =~ ${REGEX[int]} ]] &amp;&amp; SYSTEM='${RELEASE[int]}' &amp;&amp; [[ -n $SYSTEM ]] &amp;&amp; break
done

[[ -z $SYSTEM ]] &amp;&amp; red '目前暂不支持你的VPS的操作系统！' &amp;&amp; exit 1

if [[ -z $(type -P curl) ]]; then
    if [[ ! $SYSTEM == 'CentOS' ]]; then
        ${PACKAGE_UPDATE[int]}
    fi
    ${PACKAGE_INSTALL[int]} curl
fi

archAffix(){
    case '$(uname -m)' in
        x86_64 | amd64 ) echo 'amd64' ;;
        armv8 | arm64 | aarch64 ) echo 'arm64' ;;
        * ) red '不支持的CPU架构!' &amp;&amp; exit 1 ;;
    esac
}

realip(){
    ip=$(curl -s4m8 ip.sb -k) || ip=$(curl -s6m8 ip.sb -k)
}

check_ip(){
    warpv6=$(curl -s6m8 https://www.cloudflare.com/cdn-cgi/trace -k | grep warp | cut -d= -f2)
    warpv4=$(curl -s4m8 https://www.cloudflare.com/cdn-cgi/trace -k | grep warp | cut -d= -f2)
    if [[ $warpv4 =~ on|plus || $warpv6 =~ on|plus ]]; then
        wg-quick down wgcf &gt;/dev/null 2&gt;&amp;1
        systemctl stop warp-go &gt;/dev/null 2&gt;&amp;1
        realip
        systemctl start warp-go &gt;/dev/null 2&gt;&amp;1
        wg-quick up wgcf &gt;/dev/null 2&gt;&amp;1
    else
        realip
    fi
}

tuic_cert(){
    green 'Tuic 协议证书申请方式如下：'
    echo ''
    echo -e ' ${GREEN}1.${PLAIN} 必应自签证书 ${YELLOW}（默认）${PLAIN}'
    echo -e ' ${GREEN}2.${PLAIN} Acme 脚本申请'
    echo -e ' ${GREEN}3.${PLAIN} 自定义证书路径'
    echo ''
    read -rp '请输入选项 [1-2]: ' certInput
    if [[ $certInput == 3 ]]; then
        read -p '请输入公钥文件 crt 的路径：' cert_path
        yellow '公钥文件 crt 的路径：$cert_path '
        read -p '请输入密钥文件 key 的路径：' key_path
        yellow '密钥文件 key 的路径：$key_path '
        read -p '请输入证书的域名：' domain
        yellow '证书域名：$domain'
    elif [[ $certInput == 2 ]]; then
        cert_path='/root/cert.crt'
        key_path='/root/private.key'
        if [[ -f /root/cert.crt &amp;&amp; -f /root/private.key ]] &amp;&amp; [[ -s /root/cert.crt &amp;&amp; -s /root/private.key ]] &amp;&amp; [[ -f /root/ca.log ]]; then
            domain=$(cat /root/ca.log)
            green '检测到原有域名：$domain 的证书，正在应用'
        else
            WARPv4Status=$(curl -s4m8 https://www.cloudflare.com/cdn-cgi/trace -k | grep warp | cut -d= -f2)
            WARPv6Status=$(curl -s6m8 https://www.cloudflare.com/cdn-cgi/trace -k | grep warp | cut -d= -f2)
            if [[ $WARPv4Status =~ on|plus ]] || [[ $WARPv6Status =~ on|plus ]]; then
                wg-quick down wgcf &gt;/dev/null 2&gt;&amp;1
                systemctl stop warp-go &gt;/dev/null 2&gt;&amp;1
                realip
                wg-quick up wgcf &gt;/dev/null 2&gt;&amp;1
                systemctl start warp-go &gt;/dev/null 2&gt;&amp;1
            else
                realip
            fi
            
            read -p '请输入需要申请证书的域名：' domain
            [[ -z $domain ]] &amp;&amp; red '未输入域名，无法执行操作！' &amp;&amp; exit 1
            green '已输入的域名：$domain' &amp;&amp; sleep 1
            domainIP=$(dig @8.8.8.8 +time=2 +short '$domain' 2&gt;/dev/null)
            if echo $domainIP | grep -q 'network unreachable\|timed out' || [[ -z $domainIP ]]; then
                domainIP=$(dig @2001:4860:4860::8888 +time=2 aaaa +short '$domain' 2&gt;/dev/null)
            fi
            if echo $domainIP | grep -q 'network unreachable\|timed out' || [[ -z $domainIP ]] ; then
                red '未解析出 IP，请检查域名是否输入有误' 
                yellow '是否尝试强行匹配？'
                green '1. 是，将使用强行匹配'
                green '2. 否，退出脚本'
                read -p '请输入选项 [1-2]：' ipChoice
                if [[ $ipChoice == 1 ]]; then
                    yellow '将尝试强行匹配以申请域名证书'
                else
                    red '将退出脚本'
                    exit 1
                fi
            fi
            
            if [[ $domainIP == $ip ]]; then
                ${PACKAGE_INSTALL[int]} curl wget sudo socat openssl
                if [[ $SYSTEM == 'CentOS' ]]; then
                    ${PACKAGE_INSTALL[int]} cronie
                    systemctl start crond
                    systemctl enable crond
                else
                    ${PACKAGE_INSTALL[int]} cron
                    systemctl start cron
                    systemctl enable cron
                fi
                curl https://get.acme.sh | sh -s email=$(date +%s%N | md5sum | cut -c 1-16)@gmail.com
                source ~/.bashrc
                bash ~/.acme.sh/acme.sh --upgrade --auto-upgrade
                bash ~/.acme.sh/acme.sh --set-default-ca --server letsencrypt
                if [[ -n $(echo $ip | grep ':') ]]; then
                    bash ~/.acme.sh/acme.sh --issue -d ${domain} --standalone -k ec-256 --listen-v6 --insecure
                else
                    bash ~/.acme.sh/acme.sh --issue -d ${domain} --standalone -k ec-256 --insecure
                fi
                bash ~/.acme.sh/acme.sh --install-cert -d ${domain} --key-file /root/private.key --fullchain-file /root/cert.crt --ecc
                if [[ -f /root/cert.crt &amp;&amp; -f /root/private.key ]] &amp;&amp; [[ -s /root/cert.crt &amp;&amp; -s /root/private.key ]]; then
                    echo $domain &gt; /root/ca.log
                    sed -i '/--cron/d' /etc/crontab &gt;/dev/null 2&gt;&amp;1
                    echo '0 0 * * * root bash /root/.acme.sh/acme.sh --cron -f &gt;/dev/null 2&gt;&amp;1' &gt;&gt; /etc/crontab
                    green '证书申请成功! 脚本申请到的证书 (cert.crt) 和私钥 (private.key) 文件已保存到 /root 文件夹下'
                    yellow '证书crt文件路径如下: /root/cert.crt'
                    yellow '私钥key文件路径如下: /root/private.key'
                fi
            else
                red '当前域名解析的IP与当前VPS使用的真实IP不匹配'
                green '建议如下：'
                yellow '1. 请确保CloudFlare小云朵为关闭状态(仅限DNS), 其他域名解析或CDN网站设置同理'
                yellow '2. 请检查DNS解析设置的IP是否为VPS的真实IP'
                yellow '3. 脚本可能跟不上时代, 建议截图发布到GitHub Issues、GitLab Issues、论坛或TG群询问'
            fi
        fi
    else
        cert_path='/root/bing/cert.crt'
        key_path='/root/bing/private.key'
        domain='www.bing.com'

        mkdir /root/bing &amp;&amp; cd /root/bing

        openssl ecparam -genkey -name prime256v1 -out private.key
        openssl req -new -x509 -days 36500 -key private.key -out cert.crt -subj '/CN=www.bing.com'
    fi
}

tuic_port(){
    read -p '设置 tuic 端口 [1-65535]（回车则随机分配端口）：' port
    [[ -z $port ]] &amp;&amp; port=$(shuf -i 2000-65535 -n 1)
    until [[ -z $(ss -tunlp | grep -w udp | awk '{print $5}' | sed 's/.*://g' | grep -w '$port') ]]; do
        if [[ -n $(ss -tunlp | grep -w udp | awk '{print $5}' | sed 's/.*://g' | grep -w '$port') ]]; then
            echo -e '${RED} $port ${PLAIN} 端口已经被其他程序占用，请更换端口重试！'
            read -p '设置 tuic 端口 [1-65535]（回车则随机分配端口）：' port
            [[ -z $port ]] &amp;&amp; port=$(shuf -i 2000-65535 -n 1)
        fi
    done
    yellow '将在 tuic 节点使用的端口是：$port'
}

inst_tuv4(){
    check_ip

    if [[ ! ${SYSTEM} == 'CentOS' ]]; then
        ${PACKAGE_UPDATE}
        ${PACKAGE_INSTALL} bind-utils
    fi
    ${PACKAGE_INSTALL} wget curl sudo dnsutils

    wget https://gitlab.com/Misaka-blog/tuic-script/-/raw/main/files/tuic-latest-linux-$(archAffix) -O /usr/local/bin/tuic
    if [[ -f '/usr/local/bin/tuic' ]]; then
        chmod +x /usr/local/bin/tuic
    else
        red 'Tuic V4 内核安装失败！'
        exit 1
    fi

    tuic_cert
    tuic_port

    read -p '设置 tuic Token（回车跳过为随机字符）：' token
    [[ -z $token ]] &amp;&amp; token=$(date +%s%N | md5sum | cut -c 1-8)

    green '正在配置 Tuic...'
    if [[ $domain == 'www.bing.com' ]]; then
        finaldomain=$ip
        snidomain=$domain
    else
        finaldomain=$domain
        snidomain=$domain
    fi

    mkdir /etc/tuic &gt;/dev/null 2&gt;&amp;1
    cat &lt;&lt; EOF &gt; /etc/tuic/tuic.json
{
    'port': $port,
    'token': ['$token'],
    'certificate': '$cert_path',
    'private_key': '$key_path',
    'ip': '::',
    'congestion_controller': 'bbr',
    'alpn': ['h3']
}
EOF
    mkdir /root/tuic &gt;/dev/null 2&gt;&amp;1
    cat &lt;&lt; EOF &gt; /root/tuic/tuic-client.json
{
    'relay': {
        'server': '$finaldomain',
        'port': $port,
        'token': '$token',
        'ip': '$ip',
        'congestion_controller': 'bbr',
        'udp_relay_mode': 'quic',
        'alpn': ['h3'],
        'disable_sni': false,
        'reduce_rtt': false,
        'max_udp_relay_packet_size': 1500
    },
    'local': {
        'port': 6080,
        'ip': '127.0.0.1'
    },
    'log_level': 'off'
}
EOF
    cat &lt;&lt; EOF &gt; /root/tuic/tuic.txt
Sagernet 与 小火箭 配置说明（以下6项必填）：
{
    服务器地址：$finaldomain
    服务器端口：$port
    token：$token
    SNI: $snidomain
    ALPN：h3
    UDP 转发：开启
    UDP 转发模式：QUIC
    拥塞控制：bbr
    跳过服务器证书验证：开启
}
EOF
    cat &lt;&lt; EOF &gt; /root/tuic/clash-meta.yaml
mixed-port: 7890
external-controller: 127.0.0.1:9090
allow-lan: false
mode: rule
log-level: debug
ipv6: true
dns:
  enable: true
  listen: 0.0.0.0:53
  enhanced-mode: fake-ip
  nameserver:
    - 8.8.8.8
    - 1.1.1.1
    - 114.114.114.114

proxies:
  - name: Misaka-tuicV4
    server: $finaldomain
    port: $port
    type: tuic
    token: $token
    ip: $ip
    alpn: [h3]
    disable-sni: true
    reduce-rtt: true
    request-timeout: 8000
    udp-relay-mode: quic
    congestion-controller: bbr
    skip-cert-verify: true
    sni: $snidomain

proxy-groups:
  - name: Proxy
    type: select
    proxies:
      - Misaka-tuicV4
      
rules:
  - GEOIP,CN,DIRECT
  - MATCH,Proxy
EOF
    
    url='tuic://$finaldomain:$port?password=$token&amp;alpn=h3&amp;mode=bbr#tuic-misaka'
    echo ${url} &gt; /root/tuic/url.txt

    cat &lt;&lt; EOF &gt;/etc/systemd/system/tuic.service
[Unit]
Description=tuic Service
Documentation=https://gitlab.com/Misaka-blog/tuic-script
After=network.target
[Service]
User=root
ExecStart=/usr/local/bin/tuic -c /etc/tuic/tuic.json
Restart=on-failure
RestartSec=10
LimitNOFILE=infinity
[Install]
WantedBy=multi-user.target
EOF
    
    systemctl daemon-reload
    systemctl enable tuic
    systemctl start tuic

    if [[ -n $(systemctl status tuic 2&gt;/dev/null | grep -w active) &amp;&amp; -f '/etc/tuic/tuic.json' ]]; then
        green 'tuic 服务启动成功'
    else
        red 'tuic 服务启动失败，请运行 systemctl status tuic 查看服务状态并反馈，脚本退出' &amp;&amp; exit 1
    fi

    showconf
}

unst_tuv4(){
    systemctl stop tuic
    systemctl disable tuic
    rm -f /etc/systemd/system/tuic.service /root/tuic.sh
    rm -rf /usr/local/bin/tuic /etc/tuic /root/tuic
    
    green 'Tuic V4 已彻底卸载完成！'
}

inst_tuv5(){
    if [[ $(tuic -v) == '0.8.5' ]]; then
        red '检测到已安装 Tuic V4，请先卸载再安装 Tuic V5！'
        exit 1
    fi

    check_ip

    if [[ ! ${SYSTEM} == 'CentOS' ]]; then
        ${PACKAGE_UPDATE}
    fi
    ${PACKAGE_INSTALL} wget curl sudo

    wget https://gitlab.com/Misaka-blog/tuic-script/-/raw/main/files/tuic-server-latest-linux-$(archAffix) -O /usr/local/bin/tuic
    if [[ -f '/usr/local/bin/tuic' ]]; then
        chmod +x /usr/local/bin/tuic
    else
        red 'Tuic V5 内核安装失败！'
        exit 1
    fi

    tuic_cert
    tuic_port

    read -p '设置 tuic UUID（回车跳过为随机 UUID）：' uuid
    [[ -z $uuid ]] &amp;&amp; uuid=$(cat /proc/sys/kernel/random/uuid)
    yellow '使用在 tuic 节点的 UUID 为：$uuid'

    read -p '设置 tuic 密码（回车跳过为随机字符）：' passwd
    [[ -z $passwd ]] &amp;&amp; passwd=$(date +%s%N | md5sum | cut -c 1-8)
    yellow '使用在 tuic 节点的密码为：$passwd'

    green '正在配置 Tuic...'
    if [[ $domain == 'www.bing.com' ]]; then
        finaldomain=$ip
        snidomain=$domain
    else
        finaldomain=$domain
        snidomain=$domain
    fi

    mkdir /etc/tuic &gt;/dev/null 2&gt;&amp;1
    cat &lt;&lt; EOF &gt; /etc/tuic/tuic.json
{
    'server': '[::]:$port',
    'users': {
        '$uuid': '$passwd'
    },
    'certificate': '$cert_path',
    'private_key': '$key_path',
    'congestion_control': 'bbr',
    'alpn': ['h3'],
    'log_level': 'warn'
}
EOF

    mkdir /root/tuic &gt;/dev/null 2&gt;&amp;1
    cat &lt;&lt; EOF &gt; /root/tuic/tuic-client.json
{
    'relay': {
        'server': '$finaldomain:$port',
        'uuid': '$uuid',
        'password': '$passwd',
        'ip': '$ip',
        'congestion_control': 'bbr',
        'alpn': ['h3']
    },
    'local': {
        'server': '127.0.0.1:6080'
    },
    'log_level': 'warn'
}
EOF
    cat &lt;&lt; EOF &gt; /root/tuic/tuic.txt
Sagernet、Nekobox 与 小火箭 配置说明（以下6项必填）：
{
    服务器地址：$finaldomain
    服务器端口：$port
    UUID: $uuid
    密码：$passwd
    SNI: $snidomain
    ALPN：h3
    UDP 转发：开启
    UDP 转发模式：QUIC
    拥塞控制：bbr
    跳过服务器证书验证：开启
}
EOF

    url='tuic://$uuid:$passwd@$finaldomain:$port?congestion_control=bbr&amp;udp_relay_mode=quic&amp;alpn=h3#tuicv5-misaka'
    echo ${url} &gt; /root/tuic/url.txt

    cat &lt;&lt; EOF &gt; /root/tuic/clash-meta.yaml
mixed-port: 7890
external-controller: 127.0.0.1:9090
allow-lan: false
mode: rule
log-level: debug
ipv6: true
dns:
  enable: true
  listen: 0.0.0.0:53
  enhanced-mode: fake-ip
  nameserver:
    - 8.8.8.8
    - 1.1.1.1
    - 114.114.114.114

proxies:
  - name: Misaka-tuicV5
    server: $finaldomain
    port: $port
    type: tuic
    uuid: $uuid
    password: $passwd
    ip: $ip
    alpn: [h3]
    disable-sni: true
    reduce-rtt: true
    request-timeout: 8000
    udp-relay-mode: quic
    congestion-controller: bbr
    skip-cert-verify: true
    sni: $snidomain

proxy-groups:
  - name: Proxy
    type: select
    proxies:
      - Misaka-tuicV5
      
rules:
  - GEOIP,CN,DIRECT
  - MATCH,Proxy
EOF

    cat &lt;&lt; EOF &gt;/etc/systemd/system/tuic.service
[Unit]
Description=tuic Service
Documentation=https://gitlab.com/Misaka-blog/tuic-script
After=network.target
[Service]
User=root
ExecStart=/usr/local/bin/tuic -c /etc/tuic/tuic.json
Restart=on-failure
RestartSec=10
LimitNOFILE=infinity
[Install]
WantedBy=multi-user.target
EOF

    systemctl daemon-reload
    systemctl enable tuic
    systemctl start tuic
    
    if [[ -n $(systemctl status tuic 2&gt;/dev/null | grep -w active) &amp;&amp; -f '/etc/tuic/tuic.json' ]]; then
        green 'tuic 服务启动成功'
    else
        red 'tuic 服务启动失败，请运行 systemctl status tuic 查看服务状态并反馈，脚本退出' &amp;&amp; exit 1
    fi

    showconf
}

unst_tuv5(){
    systemctl stop tuic
    systemctl disable tuic
    rm -f /etc/systemd/system/tuic.service /root/tuic.sh
    rm -rf /usr/local/bin/tuic /etc/tuic /root/tuic
    
    green 'Tuic V5 已彻底卸载完成！'
}

starttuic(){
    systemctl start tuic
    systemctl enable tuic &gt;/dev/null 2&gt;&amp;1
}

stoptuic(){
    systemctl stop tuic
    systemctl disable tuic &gt;/dev/null 2&gt;&amp;1
}

tuicswitch(){
    yellow '请选择你需要的操作：'
    echo ''
    echo -e ' ${GREEN}1.${PLAIN} 启动 Tuic'
    echo -e ' ${GREEN}2.${PLAIN} 关闭 Tuic'
    echo -e ' ${GREEN}3.${PLAIN} 重启 Tuic'
    echo ''
    read -rp '请输入选项 [0-3]: ' switchInput
    case $switchInput in
        1 ) starttuic ;;
        2 ) stoptuic ;;
        3 ) stoptuic &amp;&amp; starttuic ;;
        * ) exit 1 ;;
    esac
}

changeport(){
    if [[ $(tuic -v) == '0.8.5' ]]; then
        oldport=$(cat /etc/tuic/tuic.json 2&gt;/dev/null | sed -n 2p | awk '{print $2}'| tr -d ',')

        read -p '设置 tuic 端口 [1-65535]（回车则随机分配端口）：' port
        [[ -z $port ]] &amp;&amp; port=$(shuf -i 2000-65535 -n 1)

        until [[ -z $(ss -tunlp | grep -w udp | awk '{print $5}' | sed 's/.*://g' | grep -w '$port') ]]; do
            if [[ -n $(ss -tunlp | grep -w udp | awk '{print $5}' | sed 's/.*://g' | grep -w '$port') ]]; then
                echo -e '${RED} $port ${PLAIN} 端口已经被其他程序占用，请更换端口重试！'
                read -p '设置 tuic 端口 [1-65535]（回车则随机分配端口）：' port
                [[ -z $port ]] &amp;&amp; port=$(shuf -i 2000-65535 -n 1)
            fi
        done

        sed -i '2s/$oldport/$port/g' /etc/tuic/tuic.json
        sed -i '4s/$oldport/$port/g' /root/tuic/tuic-client.json
        sed -i '4s/$oldport/$port/g' /root/tuic/tuic.txt
        sed -i '19s/$oldport/$port/g' /root/tuic/clash-meta.yaml
        sed -i 's/$oldport/$port/g' /root/tuic/url.txt

        stoptuic &amp;&amp; starttuic

        green 'Tuic V4 节点端口已成功修改为：$port'
        yellow '请手动更新客户端配置文件以使用节点'
        showconf
    else
        oldport=$(cat /etc/tuic/tuic.json 2&gt;/dev/null | sed -n 2p | awk '{print $2}' | tr -d ',' | awk -F ':' '{print $4}' | tr -d ''')
    
        read -p '设置 tuic 端口[1-65535]（回车则随机分配端口）：' port
        [[ -z $port ]] &amp;&amp; port=$(shuf -i 2000-65535 -n 1)

        until [[ -z $(ss -tunlp | grep -w udp | awk '{print $5}' | sed 's/.*://g' | grep -w '$port') ]]; do
            if [[ -n $(ss -tunlp | grep -w udp | awk '{print $5}' | sed 's/.*://g' | grep -w '$port') ]]; then
                echo -e '${RED} $port ${PLAIN} 端口已经被其他程序占用，请更换端口重试！'
                read -p '设置 tuic 端口[1-65535]（回车则随机分配端口）：' port
                [[ -z $port ]] &amp;&amp; port=$(shuf -i 2000-65535 -n 1)
            fi
        done

        sed -i '2s/$oldport/$port/g' /etc/tuic/tuic.json
        sed -i '3s/$oldport/$port/g' /root/tuic/tuic-client.json
        sed -i '4s/$oldport/$port/g' /root/tuic/tuic.txt
        sed -i '19s/$oldport/$port/g' /root/tuic/clash-meta.yaml

        stoptuic &amp;&amp; starttuic

        green 'Tuic 节点端口已成功修改为：$port'
        yellow '请手动更新客户端配置文件以使用节点'
        showconf
    fi
}

changetoken(){
    oldtoken=$(cat /etc/tuic/tuic.json 2&gt;/dev/null | sed -n 3p | awk '{print $2}' | tr -d ',[]'')

    read -p '设置tuic Token（回车跳过为随机字符）：' token
    [[ -z $token ]] &amp;&amp; token=$(date +%s%N | md5sum | cut -c 1-8)

    sed -i '3s/$oldtoken/$token/g' /etc/tuic/tuic.json
    sed -i '5s/$oldtoken/$token/g' /root/tuic/tuic-client.json
    sed -i '5s/$oldtoken/$token/g' /root/tuic/tuic.txt
    sed -i '21s/$oldtoken/$token/g' /root/tuic/clash-meta.yaml
    sed -i 's/$oldtoken/$token/g' /root/tuic/url.txt

    stoptuic &amp;&amp; starttuic

    green 'Tuic 节点 Token 已成功修改为：$token'
    yellow '请手动更新客户端配置文件以使用节点'
    showconf
}

changeuuid(){
    olduuid=$(cat /etc/tuic/tuic.json 2&gt;/dev/null | sed -n 4p | awk '{print $1}' | tr -d ':'')

    read -p '设置 tuic UUID（回车跳过为随机 UUID）：' uuid
    [[ -z $uuid ]] &amp;&amp; uuid=$(cat /proc/sys/kernel/random/uuid)

    sed -i '3s/$olduuid/$uuid/g' /etc/tuic/tuic.json
    sed -i '4s/$olduuid/$uuid/g' /root/tuic/tuic-client.json
    sed -i '5s/$olduuid/$uuid/g' /root/tuic/tuic.txt
    sed -i '21s/$olduuid/$uuid/g' /root/tuic/clash-meta.yaml

    stoptuic &amp;&amp; starttuic

    green 'Tuic 节点 UUID 已成功修改为：$uuid'
    yellow '请手动更新客户端配置文件以使用节点'
    showconf
}

changepasswd(){
    oldpasswd=$(cat /etc/tuic/tuic.json 2&gt;/dev/null | sed -n 4p | awk '{print $2}' | tr -d ''')

    read -p '设置 tuic 密码（回车跳过为随机字符）：' passwd
    [[ -z $passwd ]] &amp;&amp; passwd=$(date +%s%N | md5sum | cut -c 1-8)

    sed -i '3s/$oldpasswd/$passwd/g' /etc/tuic/tuic.json
    sed -i '5s/$oldpasswd/$passwd/g' /root/tuic/tuic-client.json
    sed -i '6s/$oldpasswd/$passwd/g' /root/tuic/tuic.txt
    sed -i '22s/$oldpasswd/$passwd/g' /root/tuic/clash-meta.yaml

    stoptuic &amp;&amp; starttuic

    green 'Tuic 节点密码已成功修改为：$passwd'
    yellow '请手动更新客户端配置文件以使用节点'
    showconf
}

changeconf(){
    if [[ $(tuic -v) == '0.8.5' ]]; then
        green 'Tuic V4 配置变更选择如下:'
        echo -e ' ${GREEN}1.${PLAIN} 修改端口'
        echo -e ' ${GREEN}2.${PLAIN} 修改Token'
        echo ''
        read -p ' 请选择操作[1-2]：' confAnswer
        case $confAnswer in
            1 ) changeport ;;
            2 ) changetoken ;;
            * ) exit 1 ;;
        esac
    else
        green 'Tuic V5 配置变更选择如下:'
        echo -e ' ${GREEN}1.${PLAIN} 修改端口'
        echo -e ' ${GREEN}2.${PLAIN} 修改 UUID'
        echo -e ' ${GREEN}3.${PLAIN} 修改密码'
        echo ''
        read -p ' 请选择操作 [1-3]：' confAnswer
        case $confAnswer in
            1 ) changeport ;;
            2 ) changeuuid ;;
            3 ) changepasswd ;;
            * ) exit 1 ;;
        esac
    fi
}

showconf(){
    yellow '客户端配置文件 tuic-client.json 内容如下，并保存到 /root/tuic/tuic-client.json'
    red '$(cat /root/tuic/tuic-client.json)'
    yellow 'Clash Meta 客户端配置文件已保存到 /root/tuic/clash-meta.yaml'
    yellow 'Tuic 节点配置明文如下，并保存到 /root/tuic/tuic.txt'
    red '$(cat /root/tuic/tuic.txt)'
    yellow 'Tuic 节点链接如下，并保存到 /root/tuic/url.txt'
    red '$(cat /root/tuic/url.txt)'
}

menu() {
    clear
    echo '#############################################################'
    echo -e '#                    ${RED}Tuic 一键安装脚本${PLAIN}                      #'
    echo -e '# ${GREEN}作者${PLAIN}: MisakaNo の 小破站                                  #'
    echo -e '# ${GREEN}博客${PLAIN}: https://blog.misaka.cyou                            #'
    echo -e '# ${GREEN}GitHub 项目${PLAIN}: https://github.com/Misaka-blog               #'
    echo -e '# ${GREEN}GitLab 项目${PLAIN}: https://gitlab.com/Misaka-blog               #'
    echo -e '# ${GREEN}Telegram 频道${PLAIN}: https://t.me/misakanocchannel              #'
    echo -e '# ${GREEN}Telegram 群组${PLAIN}: https://t.me/misakanoc                     #'
    echo -e '# ${GREEN}YouTube 频道${PLAIN}: https://www.youtube.com/@misaka-blog        #'
    echo '#############################################################'
    echo ''
    echo -e ' ${GREEN}1.${PLAIN} 安装 Tuic V4'
    echo -e ' ${GREEN}2.${PLAIN} ${RED}卸载 Tuic V4${PLAIN}'
    echo ' -------------'
    echo -e ' ${GREEN}3.${PLAIN} 安装 Tuic V5'
    echo -e ' ${GREEN}4.${PLAIN} ${RED}卸载 Tuic V5${PLAIN}'
    echo ' -------------'
    echo -e ' ${GREEN}5.${PLAIN} 关闭、启动、重启 Tuic'
    echo -e ' ${GREEN}6.${PLAIN} 修改 Tuic 配置'
    echo -e ' ${GREEN}7.${PLAIN} 显示 Tuic 配置文件'
    echo ' -------------'
    echo -e ' ${GREEN}0.${PLAIN} 退出脚本'
    echo ''
    read -rp '请输入选项 [0-7]: ' menuInput
    case $menuInput in
        1 ) inst_tuv4 ;;
        2 ) unst_tuv4 ;;
        3 ) inst_tuv5 ;;
        4 ) unst_tuv5 ;;
        5 ) tuicswitch ;;
        6 ) changeconf ;;
        7 ) showconf ;;
        * ) exit 1 ;;
    esac
}

menu
```
source
https://gitlab.com/Misaka-blog/tuic-script/-/raw/main/tuic.sh。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/TUIC%20v5-yi-jian-jiao-ben.html</guid><pubDate>Sun, 02 Nov 2025 02:23:35 +0000</pubDate></item><item><title>edgetunnel</title><link>https://doulifv.github.io/blog/post/edgetunnel.html</link><description>```
// &lt;!--GAMFC--&gt;version base on commit 58686d5d125194d34a1137913b3a64ddcf55872f, time is 2024-11-27 09:26:01 UTC&lt;!--GAMFC-END--&gt;.
// @ts-ignore
import { connect } from 'cloudflare:sockets';

// How to generate your own UUID:
// [Windows] Press 'Win + R', input cmd and run:  Powershell -NoExit -Command '[guid]::NewGuid()'
let userID = 'd342d11e-d424-4583-b36e-524ab1f0afa4';

let proxyIP = '';


if (!isValidUUID(userID)) {
	throw new Error('uuid is not valid');
}

export default {
	/**
	 * @param {import('@cloudflare/workers-types').Request} request
	 * @param {{UUID: string, PROXYIP: string}} env
	 * @param {import('@cloudflare/workers-types').ExecutionContext} ctx
	 * @returns {Promise&lt;Response&gt;}
	 */
	async fetch(request, env, ctx) {
		try {
			userID = env.UUID || userID;
			proxyIP = env.PROXYIP || proxyIP;
			const upgradeHeader = request.headers.get('Upgrade');
			if (!upgradeHeader || upgradeHeader !== 'websocket') {
				const url = new URL(request.url);
				switch (url.pathname) {
					case '/':
						return new Response(JSON.stringify(request.cf), { status: 200 });
					case `/${userID}`: {
						const vlessConfig = getVLESSConfig(userID, request.headers.get('Host'));
						return new Response(`${vlessConfig}`, {
							status: 200,
							headers: {
								'Content-Type': 'text/plain;charset=utf-8',
							}
						});
					}
					default:
						return new Response('Not found', { status: 404 });
				}
			} else {
				return await vlessOverWSHandler(request);
			}
		} catch (err) {
			/** @type {Error} */ let e = err;
			return new Response(e.toString());
		}
	},
};




/**
 * 
 * @param {import('@cloudflare/workers-types').Request} request
 */
async function vlessOverWSHandler(request) {

	/** @type {import('@cloudflare/workers-types').WebSocket[]} */
	// @ts-ignore
	const webSocketPair = new WebSocketPair();
	const [client, webSocket] = Object.values(webSocketPair);

	webSocket.accept();

	let address = '';
	let portWithRandomLog = '';
	const log = (/** @type {string} */ info, /** @type {string | undefined} */ event) =&gt; {
		console.log(`[${address}:${portWithRandomLog}] ${info}`, event || '');
	};
	const earlyDataHeader = request.headers.get('sec-websocket-protocol') || '';

	const readableWebSocketStream = makeReadableWebSocketStream(webSocket, earlyDataHeader, log);

	/** @type {{ value: import('@cloudflare/workers-types').Socket | null}}*/
	let remoteSocketWapper = {
		value: null,
	};
	let udpStreamWrite = null;
	let isDns = false;

	// ws --&gt; remote
	readableWebSocketStream.pipeTo(new WritableStream({
		async write(chunk, controller) {
			if (isDns &amp;&amp; udpStreamWrite) {
				return udpStreamWrite(chunk);
			}
			if (remoteSocketWapper.value) {
				const writer = remoteSocketWapper.value.writable.getWriter()
				await writer.write(chunk);
				writer.releaseLock();
				return;
			}

			const {
				hasError,
				message,
				portRemote = 443,
				addressRemote = '',
				rawDataIndex,
				vlessVersion = new Uint8Array([0, 0]),
				isUDP,
			} = processVlessHeader(chunk, userID);
			address = addressRemote;
			portWithRandomLog = `${portRemote}--${Math.random()} ${isUDP ? 'udp ' : 'tcp '
				} `;
			if (hasError) {
				// controller.error(message);
				throw new Error(message); // cf seems has bug, controller.error will not end stream
				// webSocket.close(1000, message);
				return;
			}
			// if UDP but port not DNS port, close it
			if (isUDP) {
				if (portRemote === 53) {
					isDns = true;
				} else {
					// controller.error('UDP proxy only enable for DNS which is port 53');
					throw new Error('UDP proxy only enable for DNS which is port 53'); // cf seems has bug, controller.error will not end stream
					return;
				}
			}
			// ['version', '附加信息长度 N']
			const vlessResponseHeader = new Uint8Array([vlessVersion[0], 0]);
			const rawClientData = chunk.slice(rawDataIndex);

			// TODO: support udp here when cf runtime has udp support
			if (isDns) {
				const { write } = await handleUDPOutBound(webSocket, vlessResponseHeader, log);
				udpStreamWrite = write;
				udpStreamWrite(rawClientData);
				return;
			}
			handleTCPOutBound(remoteSocketWapper, addressRemote, portRemote, rawClientData, webSocket, vlessResponseHeader, log);
		},
		close() {
			log(`readableWebSocketStream is close`);
		},
		abort(reason) {
			log(`readableWebSocketStream is abort`, JSON.stringify(reason));
		},
	})).catch((err) =&gt; {
		log('readableWebSocketStream pipeTo error', err);
	});

	return new Response(null, {
		status: 101,
		// @ts-ignore
		webSocket: client,
	});
}

/**
 * Handles outbound TCP connections.
 *
 * @param {any} remoteSocket 
 * @param {string} addressRemote The remote address to connect to.
 * @param {number} portRemote The remote port to connect to.
 * @param {Uint8Array} rawClientData The raw client data to write.
 * @param {import('@cloudflare/workers-types').WebSocket} webSocket The WebSocket to pass the remote socket to.
 * @param {Uint8Array} vlessResponseHeader The VLESS response header.
 * @param {function} log The logging function.
 * @returns {Promise&lt;void&gt;} The remote socket.
 */
async function handleTCPOutBound(remoteSocket, addressRemote, portRemote, rawClientData, webSocket, vlessResponseHeader, log,) {
	async function connectAndWrite(address, port) {
		/** @type {import('@cloudflare/workers-types').Socket} */
		const tcpSocket = connect({
			hostname: address,
			port: port,
		});
		remoteSocket.value = tcpSocket;
		log(`connected to ${address}:${port}`);
		const writer = tcpSocket.writable.getWriter();
		await writer.write(rawClientData); // first write, nomal is tls client hello
		writer.releaseLock();
		return tcpSocket;
	}

	// if the cf connect tcp socket have no incoming data, we retry to redirect ip
	async function retry() {
		const tcpSocket = await connectAndWrite(proxyIP || addressRemote, portRemote)
		// no matter retry success or not, close websocket
		tcpSocket.closed.catch(error =&gt; {
			console.log('retry tcpSocket closed error', error);
		}).finally(() =&gt; {
			safeCloseWebSocket(webSocket);
		})
		remoteSocketToWS(tcpSocket, webSocket, vlessResponseHeader, null, log);
	}

	const tcpSocket = await connectAndWrite(addressRemote, portRemote);

	// when remoteSocket is ready, pass to websocket
	// remote--&gt; ws
	remoteSocketToWS(tcpSocket, webSocket, vlessResponseHeader, retry, log);
}

/**
 * 
 * @param {import('@cloudflare/workers-types').WebSocket} webSocketServer
 * @param {string} earlyDataHeader for ws 0rtt
 * @param {(info: string)=&gt; void} log for ws 0rtt
 */
function makeReadableWebSocketStream(webSocketServer, earlyDataHeader, log) {
	let readableStreamCancel = false;
	const stream = new ReadableStream({
		start(controller) {
			webSocketServer.addEventListener('message', (event) =&gt; {
				if (readableStreamCancel) {
					return;
				}
				const message = event.data;
				controller.enqueue(message);
			});

			// The event means that the client closed the client -&gt; server stream.
			// However, the server -&gt; client stream is still open until you call close() on the server side.
			// The WebSocket protocol says that a separate close message must be sent in each direction to fully close the socket.
			webSocketServer.addEventListener('close', () =&gt; {
				// client send close, need close server
				// if stream is cancel, skip controller.close
				safeCloseWebSocket(webSocketServer);
				if (readableStreamCancel) {
					return;
				}
				controller.close();
			}
			);
			webSocketServer.addEventListener('error', (err) =&gt; {
				log('webSocketServer has error');
				controller.error(err);
			}
			);
			// for ws 0rtt
			const { earlyData, error } = base64ToArrayBuffer(earlyDataHeader);
			if (error) {
				controller.error(error);
			} else if (earlyData) {
				controller.enqueue(earlyData);
			}
		},

		pull(controller) {
			// if ws can stop read if stream is full, we can implement backpressure
			// https://streams.spec.whatwg.org/#example-rs-push-backpressure
		},
		cancel(reason) {
			// 1. pipe WritableStream has error, this cancel will called, so ws handle server close into here
			// 2. if readableStream is cancel, all controller.close/enqueue need skip,
			// 3. but from testing controller.error still work even if readableStream is cancel
			if (readableStreamCancel) {
				return;
			}
			log(`ReadableStream was canceled, due to ${reason}`)
			readableStreamCancel = true;
			safeCloseWebSocket(webSocketServer);
		}
	});

	return stream;

}

// https://xtls.github.io/development/protocols/vless.html
// https://github.com/zizifn/excalidraw-backup/blob/main/v2ray-protocol.excalidraw

/**
 * 
 * @param { ArrayBuffer} vlessBuffer 
 * @param {string} userID 
 * @returns 
 */
function processVlessHeader(
	vlessBuffer,
	userID
) {
	if (vlessBuffer.byteLength &lt; 24) {
		return {
			hasError: true,
			message: 'invalid data',
		};
	}
	const version = new Uint8Array(vlessBuffer.slice(0, 1));
	let isValidUser = false;
	let isUDP = false;
	if (stringify(new Uint8Array(vlessBuffer.slice(1, 17))) === userID) {
		isValidUser = true;
	}
	if (!isValidUser) {
		return {
			hasError: true,
			message: 'invalid user',
		};
	}

	const optLength = new Uint8Array(vlessBuffer.slice(17, 18))[0];
	//skip opt for now

	const command = new Uint8Array(
		vlessBuffer.slice(18 + optLength, 18 + optLength + 1)
	)[0];

	// 0x01 TCP
	// 0x02 UDP
	// 0x03 MUX
	if (command === 1) {
	} else if (command === 2) {
		isUDP = true;
	} else {
		return {
			hasError: true,
			message: `command ${command} is not support, command 01-tcp,02-udp,03-mux`,
		};
	}
	const portIndex = 18 + optLength + 1;
	const portBuffer = vlessBuffer.slice(portIndex, portIndex + 2);
	// port is big-Endian in raw data etc 80 == 0x005d
	const portRemote = new DataView(portBuffer).getUint16(0);

	let addressIndex = portIndex + 2;
	const addressBuffer = new Uint8Array(
		vlessBuffer.slice(addressIndex, addressIndex + 1)
	);

	// 1--&gt; ipv4  addressLength =4
	// 2--&gt; domain name addressLength=addressBuffer[1]
	// 3--&gt; ipv6  addressLength =16
	const addressType = addressBuffer[0];
	let addressLength = 0;
	let addressValueIndex = addressIndex + 1;
	let addressValue = '';
	switch (addressType) {
		case 1:
			addressLength = 4;
			addressValue = new Uint8Array(
				vlessBuffer.slice(addressValueIndex, addressValueIndex + addressLength)
			).join('.');
			break;
		case 2:
			addressLength = new Uint8Array(
				vlessBuffer.slice(addressValueIndex, addressValueIndex + 1)
			)[0];
			addressValueIndex += 1;
			addressValue = new TextDecoder().decode(
				vlessBuffer.slice(addressValueIndex, addressValueIndex + addressLength)
			);
			break;
		case 3:
			addressLength = 16;
			const dataView = new DataView(
				vlessBuffer.slice(addressValueIndex, addressValueIndex + addressLength)
			);
			// 2001:0db8:85a3:0000:0000:8a2e:0370:7334
			const ipv6 = [];
			for (let i = 0; i &lt; 8; i++) {
				ipv6.push(dataView.getUint16(i * 2).toString(16));
			}
			addressValue = ipv6.join(':');
			// seems no need add [] for ipv6
			break;
		default:
			return {
				hasError: true,
				message: `invild  addressType is ${addressType}`,
			};
	}
	if (!addressValue) {
		return {
			hasError: true,
			message: `addressValue is empty, addressType is ${addressType}`,
		};
	}

	return {
		hasError: false,
		addressRemote: addressValue,
		addressType,
		portRemote,
		rawDataIndex: addressValueIndex + addressLength,
		vlessVersion: version,
		isUDP,
	};
}


/**
 * 
 * @param {import('@cloudflare/workers-types').Socket} remoteSocket 
 * @param {import('@cloudflare/workers-types').WebSocket} webSocket 
 * @param {ArrayBuffer} vlessResponseHeader 
 * @param {(() =&gt; Promise&lt;void&gt;) | null} retry
 * @param {*} log 
 */
async function remoteSocketToWS(remoteSocket, webSocket, vlessResponseHeader, retry, log) {
	// remote--&gt; ws
	let remoteChunkCount = 0;
	let chunks = [];
	/** @type {ArrayBuffer | null} */
	let vlessHeader = vlessResponseHeader;
	let hasIncomingData = false; // check if remoteSocket has incoming data
	await remoteSocket.readable
		.pipeTo(
			new WritableStream({
				start() {
				},
				/**
				 * 
				 * @param {Uint8Array} chunk 
				 * @param {*} controller 
				 */
				async write(chunk, controller) {
					hasIncomingData = true;
					// remoteChunkCount++;
					if (webSocket.readyState !== WS_READY_STATE_OPEN) {
						controller.error(
							'webSocket.readyState is not open, maybe close'
						);
					}
					if (vlessHeader) {
						webSocket.send(await new Blob([vlessHeader, chunk]).arrayBuffer());
						vlessHeader = null;
					} else {
						// seems no need rate limit this, CF seems fix this??..
						// if (remoteChunkCount &gt; 20000) {
						// 	// cf one package is 4096 byte(4kb),  4096 * 20000 = 80M
						// 	await delay(1);
						// }
						webSocket.send(chunk);
					}
				},
				close() {
					log(`remoteConnection!.readable is close with hasIncomingData is ${hasIncomingData}`);
					// safeCloseWebSocket(webSocket); // no need server close websocket frist for some case will casue HTTP ERR_CONTENT_LENGTH_MISMATCH issue, client will send close event anyway.
				},
				abort(reason) {
					console.error(`remoteConnection!.readable abort`, reason);
				},
			})
		)
		.catch((error) =&gt; {
			console.error(
				`remoteSocketToWS has exception `,
				error.stack || error
			);
			safeCloseWebSocket(webSocket);
		});

	// seems is cf connect socket have error,
	// 1. Socket.closed will have error
	// 2. Socket.readable will be close without any data coming
	if (hasIncomingData === false &amp;&amp; retry) {
		log(`retry`)
		retry();
	}
}

/**
 * 
 * @param {string} base64Str 
 * @returns 
 */
function base64ToArrayBuffer(base64Str) {
	if (!base64Str) {
		return { error: null };
	}
	try {
		// go use modified Base64 for URL rfc4648 which js atob not support
		base64Str = base64Str.replace(/-/g, '+').replace(/_/g, '/');
		const decode = atob(base64Str);
		const arryBuffer = Uint8Array.from(decode, (c) =&gt; c.charCodeAt(0));
		return { earlyData: arryBuffer.buffer, error: null };
	} catch (error) {
		return { error };
	}
}

/**
 * This is not real UUID validation
 * @param {string} uuid 
 */
function isValidUUID(uuid) {
	const uuidRegex = /^[0-9a-f]{8}-[0-9a-f]{4}-[4][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
	return uuidRegex.test(uuid);
}

const WS_READY_STATE_OPEN = 1;
const WS_READY_STATE_CLOSING = 2;
/**
 * Normally, WebSocket will not has exceptions when close.
 * @param {import('@cloudflare/workers-types').WebSocket} socket
 */
function safeCloseWebSocket(socket) {
	try {
		if (socket.readyState === WS_READY_STATE_OPEN || socket.readyState === WS_READY_STATE_CLOSING) {
			socket.close();
		}
	} catch (error) {
		console.error('safeCloseWebSocket error', error);
	}
}

const byteToHex = [];
for (let i = 0; i &lt; 256; ++i) {
	byteToHex.push((i + 256).toString(16).slice(1));
}
function unsafeStringify(arr, offset = 0) {
	return (byteToHex[arr[offset + 0]] + byteToHex[arr[offset + 1]] + byteToHex[arr[offset + 2]] + byteToHex[arr[offset + 3]] + '-' + byteToHex[arr[offset + 4]] + byteToHex[arr[offset + 5]] + '-' + byteToHex[arr[offset + 6]] + byteToHex[arr[offset + 7]] + '-' + byteToHex[arr[offset + 8]] + byteToHex[arr[offset + 9]] + '-' + byteToHex[arr[offset + 10]] + byteToHex[arr[offset + 11]] + byteToHex[arr[offset + 12]] + byteToHex[arr[offset + 13]] + byteToHex[arr[offset + 14]] + byteToHex[arr[offset + 15]]).toLowerCase();
}
function stringify(arr, offset = 0) {
	const uuid = unsafeStringify(arr, offset);
	if (!isValidUUID(uuid)) {
		throw TypeError('Stringified UUID is invalid');
	}
	return uuid;
}


/**
 * 
 * @param {import('@cloudflare/workers-types').WebSocket} webSocket 
 * @param {ArrayBuffer} vlessResponseHeader 
 * @param {(string)=&gt; void} log 
 */
async function handleUDPOutBound(webSocket, vlessResponseHeader, log) {

	let isVlessHeaderSent = false;
	const transformStream = new TransformStream({
		start(controller) {

		},
		transform(chunk, controller) {
			// udp message 2 byte is the the length of udp data
			// TODO: this should have bug, beacsue maybe udp chunk can be in two websocket message
			for (let index = 0; index &lt; chunk.byteLength;) {
				const lengthBuffer = chunk.slice(index, index + 2);
				const udpPakcetLength = new DataView(lengthBuffer).getUint16(0);
				const udpData = new Uint8Array(
					chunk.slice(index + 2, index + 2 + udpPakcetLength)
				);
				index = index + 2 + udpPakcetLength;
				controller.enqueue(udpData);
			}
		},
		flush(controller) {
		}
	});

	// only handle dns udp for now
	transformStream.readable.pipeTo(new WritableStream({
		async write(chunk) {
			const resp = await fetch('https://1.1.1.1/dns-query',
				{
					method: 'POST',
					headers: {
						'content-type': 'application/dns-message',
					},
					body: chunk,
				})
			const dnsQueryResult = await resp.arrayBuffer();
			const udpSize = dnsQueryResult.byteLength;
			// console.log([...new Uint8Array(dnsQueryResult)].map((x) =&gt; x.toString(16)));
			const udpSizeBuffer = new Uint8Array([(udpSize &gt;&gt; 8) &amp; 0xff, udpSize &amp; 0xff]);
			if (webSocket.readyState === WS_READY_STATE_OPEN) {
				log(`doh success and dns message length is ${udpSize}`);
				if (isVlessHeaderSent) {
					webSocket.send(await new Blob([udpSizeBuffer, dnsQueryResult]).arrayBuffer());
				} else {
					webSocket.send(await new Blob([vlessResponseHeader, udpSizeBuffer, dnsQueryResult]).arrayBuffer());
					isVlessHeaderSent = true;
				}
			}
		}
	})).catch((error) =&gt; {
		log('dns udp has error' + error)
	});

	const writer = transformStream.writable.getWriter();

	return {
		/**
		 * 
		 * @param {Uint8Array} chunk 
		 */
		write(chunk) {
			writer.write(chunk);
		}
	};
}

/**
 * 
 * @param {string} userID 
 * @param {string | null} hostName
 * @returns {string}
 */
function getVLESSConfig(userID, hostName) {
	const protocol = 'vless';
	const vlessMain = 
	`${protocol}` + 
	`://${userID}@${hostName}:443`+
	`?encryption=none&amp;security=tls&amp;sni=${hostName}&amp;fp=randomized&amp;type=ws&amp;host=${hostName}&amp;path=%2F%3Fed%3D2048#${hostName}`;
	
	return `
################################################################
v2ray
---------------------------------------------------------------
${vlessMain}
---------------------------------------------------------------
################################################################
clash-meta
---------------------------------------------------------------
- type: vless
  name: ${hostName}
  server: ${hostName}
  port: 443
  uuid: ${userID}
  network: ws
  tls: true
  udp: false
  sni: ${hostName}
  client-fingerprint: chrome
  ws-opts:
    path: '/?ed=2048'
    headers:
      host: ${hostName}
---------------------------------------------------------------
################################################################
`;
}

```
[[edgetunnel](https://github.com/zizifn/edgetunnel)](https://github.com/zizifn/edgetunnel)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/edgetunnel.html</guid><pubDate>Sat, 01 Nov 2025 12:39:58 +0000</pubDate></item><item><title>笔记本禁用自带键盘</title><link>https://doulifv.github.io/blog/post/bi-ji-ben-jin-yong-zi-dai-jian-pan.html</link><description>禁用自带键盘 **（需要管理员权限）** 
```
sc config i8042prt start= disabled
```
恢复启用自带键盘 **（同样需要管理员权限）** 
```
sc config i8042prt start= auto
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/bi-ji-ben-jin-yong-zi-dai-jian-pan.html</guid><pubDate>Wed, 29 Oct 2025 08:43:34 +0000</pubDate></item><item><title>Linux时间同步</title><link>https://doulifv.github.io/blog/post/Linux-shi-jian-tong-bu.html</link><description>同步系统时间
chronyd 命令可让您检查系统时钟关闭的时间。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Linux-shi-jian-tong-bu.html</guid><pubDate>Sun, 26 Oct 2025 11:49:27 +0000</pubDate></item><item><title>Markdown学习</title><link>https://doulifv.github.io/blog/post/Markdown-xue-xi.html</link><description>我转了=我会了 ( : 嗯嗯
这是一个markdown格式的测试页面，也是个人经常会使用的格式记录。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Markdown-xue-xi.html</guid><pubDate>Sat, 25 Oct 2025 14:12:55 +0000</pubDate></item><item><title>windwos 永久激活脚本</title><link>https://doulifv.github.io/blog/post/windwos%20-yong-jiu-ji-huo-jiao-ben.html</link><description>实测适用于WINDOWS 11 10 8 7 
**在PowerShell中输入以安装**
```
irm https://get.activated.win | iex
```
&gt;**Source**
&gt;[https://github.com/massgravel/Microsoft-Activation-Scripts](https://github.com/massgravel/Microsoft-Activation-Scripts)
&gt;**Maybe you can download this file run,If you need offline use.**。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/windwos%20-yong-jiu-ji-huo-jiao-ben.html</guid><pubDate>Sat, 25 Oct 2025 06:52:59 +0000</pubDate></item><item><title>收藏夹</title><link>https://doulifv.github.io/blog/post/shou-cang-jia.html</link><description>[Z哥视频精选](https://space.bilibili.com/453345074/channel/collectiondetail?sid=6560672&amp;spm_id_from=333.788.0.0)
&gt;不追，不动，不慌，不乱摸，知行合一
&gt;踏踏实实、拿具不动

[一个人翻身最快的方法：换地方](https://mp.weixin.qq.com/s/0Q1IbzxQTf8_IlZstiU1gw)
&gt;人困在一个地方，如同一潭死水，慢慢会失去活力。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/shou-cang-jia.html</guid><pubDate>Sun, 19 Oct 2025 01:58:30 +0000</pubDate></item><item><title>VPS上使用的脚本</title><link>https://doulifv.github.io/blog/post/VPS-shang-shi-yong-de-jiao-ben.html</link><description>查看IP信息
```
curl ip.im/info
```
IP质检
```
bash &lt;(curl -sL IP.Check.Place)
```
```
bash &lt;(curl -sL Net.Check.Place)
```
233boy sing-box一键脚本
```
bash &lt;(wget -qO- -o- https://github.com/233boy/sing-box/raw/main/install.sh)
```
设置变量ip=192.168.1.2适用于申请ssl证书套了warp出口ip与本地ip不一致的情况
```
export ip=192.168.1.2
```
http代理
```
export http_proxy=http://192.168.1.2:12334
```
socks5代理
```
export http_proxy='socks5://127.0.0.1:1080'
```
**more writing...is soon**。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/VPS-shang-shi-yong-de-jiao-ben.html</guid><pubDate>Sat, 18 Oct 2025 01:58:35 +0000</pubDate></item><item><title>2053端口弱密码扫描</title><link>https://doulifv.github.io/blog/post/2053-duan-kou-ruo-mi-ma-sao-miao.html</link><description>```
#!/bin/bash
#echo 'IP range:'
#read ip_range
ip_range=155
#echo 'Port'
#read port
port=2053
#echo 'IP range&amp; Port=${ip_range}.0.0.0:${port}'
masscan ${ip_range}.0.0.0/8 -p${port} --banners --rate 10000 --exclude 255.255.255.255 -oJ scan.json

for ip_ad in $(sed -nE 's/.*'ip': '([^']+)'.*/\1/p' scan.json); do
    if curl --max-time 1 http://${ip_ad}:${port}; then
        res=$(curl 'http://${ip_ad}:${port}/login' --data-raw 'username=admin&amp;password=admin' --compressed --insecure)
        if [[ '$res' =~ .*:true,.* ]]; then
            echo ${ip_ad}:${port} | tee -a week.txt
        fi
        echo ${ip_ad}:${port} | tee -a all.log
    fi
done;
```
依赖的包curl、masscan配合使用nohup后台运行。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/2053-duan-kou-ruo-mi-ma-sao-miao.html</guid><pubDate>Sat, 18 Oct 2025 01:47:01 +0000</pubDate></item><item><title>解决Steam Deck首次开机卡更新情况</title><link>https://doulifv.github.io/blog/post/jie-jue-Steam%20Deck-shou-ci-kai-ji-qia-geng-xin-qing-kuang.html</link><description>实测挂代理没有什么用处，网上搜索了一通找到这个方法更新成功

详细讲解操作办法：

1.如果你已经进入图形界面的更新过程，那么请你现在长按电源键，重启你的deck

2.正常设置语言，没有限制一定要英文，选你喜欢的语言和时区，美国时间、日本时间、中国时间、韩国时间；中日英韩语，甚至是其他你看得懂的语言都OK，选好后进入下一步，设置网络

3.（请将第4步的所有文字看完再操作！）

4.设置网络的时候，不要急着下一步！设置好网络之后，屏幕状态栏右上角显示Wi-Fi或有线网络的图标。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/jie-jue-Steam%20Deck-shou-ci-kai-ji-qia-geng-xin-qing-kuang.html</guid><pubDate>Sun, 12 Oct 2025 13:10:18 +0000</pubDate></item><item><title>Stardew Valley 星露谷mod备份</title><link>https://doulifv.github.io/blog/post/Stardew%20Valley%20-xing-lu-gu-mod-bei-fen.html</link><description>## 万一换电脑别给我整没了提前记录备份
 &lt;div align='center'&gt;&lt;img src='https://smapi.io/Content/images/pufferchick.svg'&gt;&lt;/div&gt;

#### [SMAPI:万物之源，没有这个你啥也干不了](https://smapi.io/)  
#### [UIInfoSuite2:UI信息显示幸运值、物品价格、天气预报等 ](https://github.com/Annosz/UIInfoSuite2)
#### [NPCMapLocations:用来显示NPC的实时位置信息](https://www.nexusmods.com/stardewvalley/mods/239)
#### [Lookup Anything:用来显示NPC的喜好物品属性](https://www.nexusmods.com/stardewvalley/mods/541)
。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Stardew%20Valley%20-xing-lu-gu-mod-bei-fen.html</guid><pubDate>Sun, 12 Oct 2025 12:41:00 +0000</pubDate></item><item><title>NAS软件推荐</title><link>https://doulifv.github.io/blog/post/NAS-ruan-jian-tui-jian.html</link><description>web硬盘管理工具
[cockpit](https://cockpit-project.org/running.html)
直播录制工具
[/bililive-go](https://github.com/bililive-go/bililive-go)
开源Linux运维web面板
[1panel](https://1panel.cn/)
磁力下载
[qBittorrent](https://github.com/qbittorrent/qBittorrent)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/NAS-ruan-jian-tui-jian.html</guid><pubDate>Sat, 11 Oct 2025 07:47:42 +0000</pubDate></item><item><title>Debian apt更新https源缺少公钥问题</title><link>https://doulifv.github.io/blog/post/Debian%20apt-geng-xin-https-yuan-que-shao-gong-yao-wen-ti.html</link><description>
&gt;root@debian:~# apt update
Get:1 http://repo.mysql.com/apt/debian buster InRelease [22.1 kB]     
Hit:2 http://mirrors4.tuna.tsinghua.edu.cn/debian buster InRelease    
Hit:3 http://mirrors4.tuna.tsinghua.edu.cn/debian buster-updates InRelease
Get:4 http://mirrors4.tuna.tsinghua.edu.cn/debian buster-backports InRelease [51.4 kB]
Err:1 http://repo.mysql.com/apt/debian buster InRelease
  The following signatures couldn't be verified because the public key is not available: NO_PUBKEY **467B942D3A79BD29**
Get:5 http://mirrors4.tuna.tsinghua.edu.cn/debian-security buster/updates InRelease [34.8 kB]
Reading package lists... Done      
W: GPG error: http://repo.mysql.com/apt/debian buster InRelease: The following signatures couldn't be verified because the public key is not available: NO_PUBKEY 467B942D3A79BD29
E: The repository 'http://repo.mysql.com/apt/debian buster InRelease' is not signed.
N: Updating from such a repository can't be done securely, and is therefore disabled by default.
N: See apt-secure(8) manpage for repository creation and user configuration details.

添加
```
apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv 467B942D3A79BD29
```
&gt;root@debian:~# apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv 467B942D3A79BD29
Executing: /tmp/apt-key-gpghome.BKquJCirKG/gpg.1.sh --keyserver hkp://keyserver.ubuntu.com:80 --recv 467B942D3A79BD29
gpg: key 467B942D3A79BD29: public key 'MySQL Release Engineering &lt;mysql-build@oss.oracle.com&gt;' imported
gpg: Total number processed: 1
gpg:               imported: 1

此时再更新就没有问题了

参考:[#解决 由于没有公钥，无法验证下列签名 :NO_PUBKEY-博客园](https://www.cnblogs.com/2205254761qq/p/11863928.html)
。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Debian%20apt-geng-xin-https-yuan-que-shao-gong-yao-wen-ti.html</guid><pubDate>Sat, 11 Oct 2025 05:54:39 +0000</pubDate></item><item><title>解决Debian键盘错乱问题</title><link>https://doulifv.github.io/blog/post/jie-jue-Debian-jian-pan-cuo-luan-wen-ti.html</link><description>1.编辑vimrc.tiny 文件
```
vi /etc/vim/vimrc.tiny 
```
2.修改、增加以下两行内容
```
set nocompatible #修改
set backspace=2 #增加
```
3.保存退出即可

[vim 方向键和backspace乱码-CSDN](https://blog.csdn.net/qq_36340642/article/details/109806721)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/jie-jue-Debian-jian-pan-cuo-luan-wen-ti.html</guid><pubDate>Sat, 11 Oct 2025 05:54:09 +0000</pubDate></item><item><title>手动使用Acme.sh证书脚本申请SSL证书</title><link>https://doulifv.github.io/blog/post/shou-dong-shi-yong-Acme.sh-zheng-shu-jiao-ben-shen-qing-SSL-zheng-shu.html</link><description>
#准备材料
一台VPS
一枚域名
CloudFlare账号（如使用CF API）

#部署步骤
###安装Acme.sh证书申请脚本
1.运行以下命令，安装Acme.sh证书申请脚本
```
curl https://get.acme.sh | sh -s email=my@example.com
```
&gt;由于Acme.sh脚本调用了GitHub的资源，且GitHub不支持纯IPv6的环境，所以请自行设置DNS64或安装WARP解决
建议替换[my@example.com](mailto:my@example.com)为自己的邮箱
2.切换默认证书签发的CA机构为下面三家的任何一家

Let’s Encrypt （签发速度快、稳定）
```
bash ~/.acme.sh/acme.sh --set-default-ca --server letsencrypt
```
ZeroSSL （稳定、但部分情况无法签发）
```
bash ~/.acme.sh/acme.sh --set-default-ca --server zerossl
```
Buypass（没试过）
```
bash ~/.acme.sh/acme.sh --set-default-ca --server buypass
```
3.设置Acme.sh自动更新，始终与官方保持一致
```
bash ~/.acme.sh/acme.sh --upgrade --auto-upgrade
```
###使用Standalone模式申请SSL证书
此方法使用之前请确保80端口畅通，并且域名已经事先解析到VPS的IP
域名解析至IPv4：
```
bash ~/.acme.sh/acme.sh --issue -d '域名' --standalone -k ec-256
```
域名已解析至IPv6：
```
bash ~/.acme.sh/acme.sh --issue -d '域名' --standalone -k ec-256 --listen-v6
```
###使用CloudFlare API Key申请SSL证书
此方法可以使用泛域名、无需DNS解析，但由于受到CF API限制，不可适用于Freenom系列的免费域名
1.设置CloudFlare Global API Key和登录邮箱
```
export CF_Key='你自己的CloudFlare Global API Key'
export CF_Email='你自己的CloudFlare账户登录邮箱'
```
2.运行一下命令
####单域名
IPv4 或原生双栈VPS
```
bash ~/.acme.sh/acme.sh --issue --dns dns_cf -d '域名' -k ec-256
```
IPv6
```
bash ~/.acme.sh/acme.sh --issue --dns dns_cf -d '域名' -k ec-256 --listen-v6
```
####泛域名
IPv4 或原生双栈VPS
```
bash ~/.acme.sh/acme.sh --issue --dns dns_cf -d '*.域名' -d '域名' -k ec-256
```
IPv6
```
bash ~/.acme.sh/acme.sh --issue --dns dns_cf -d '*.域名' -d '域名' -k ec-256 --listen-v6
```
####安装域名证书
由于acme.sh的证书不能直接使用，因此我们需要安装证书
单域名
```
bash ~/.acme.sh/acme.sh --install-cert -d '域名' --key-file /root/private.key --fullchain-file /root/cert.crt --ecc
```
泛域名
```
bash ~/.acme.sh/acme.sh --install-cert -d '*.域名' --key-file /root/private.key --fullchain-file /root/cert.crt --ecc
```
&gt;运行此命令后，证书crt路径为/root/cert.crt，私钥key路径为/root/private.key，可自行修改

####查看目前申请的证书
```
bash ~/.acme.sh/acme.sh --list
```
####撤销目前申请的证书
```
bash ~/.acme.sh/acme.sh --revoke -d '域名' --ecc
bash ~/.acme.sh/acme.sh --remove -d '域名' --ecc
```
####手动续期证书
```
bash ~/.acme.sh/acme.sh --renew -d '域名' --force --ecc
```
####卸载Acme.sh脚本
```
bash ~/.acme.sh/acme.sh --uninstall
```
[[DELETE]手动使用Acme.sh证书脚本申请SSL证书 | MisakaNo の 小破站](https://blog.misaka.rest/2023/04/02/acme-ssl-cert/?highlight=acme)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/shou-dong-shi-yong-Acme.sh-zheng-shu-jiao-ben-shen-qing-SSL-zheng-shu.html</guid><pubDate>Sat, 11 Oct 2025 05:53:37 +0000</pubDate></item><item><title>Debian 系统安装 MySQL</title><link>https://doulifv.github.io/blog/post/Debian%20-xi-tong-an-zhuang-%20MySQL.html</link><description>首先安装需要用到的依赖
```
apt install wget gnupg lsb-release -y
```
使用wget获取deb包、安装  | 需要其他版本可前往[MySQL官网下载](https://dev.mysql.com/downloads/)
```
wget https://dev.mysql.com/get/mysql-apt-config_0.8.28-1_all.deb
dpkg -i mysql-apt-config_0.8.28-1_all.deb 
```
![选择OK即可](https://upload-images.jianshu.io/upload_images/27021437-9f963c9264a73d55.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
更新软件包、安装mysql-server
```
apt update
apt install mysql-server
```
![设置一个可靠的密码](https://upload-images.jianshu.io/upload_images/27021437-2cd8bc2131ef6f72.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)
至此MySQL服务已经部署完毕。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Debian%20-xi-tong-an-zhuang-%20MySQL.html</guid><pubDate>Sat, 11 Oct 2025 05:53:05 +0000</pubDate></item><item><title>Debian 12 更改apt源</title><link>https://doulifv.github.io/blog/post/Debian%2012%20-geng-gai-apt-yuan.html</link><description>更新源命令
```
apt-get update 
apt-get upgrade 
```
[可参考清华大学镜像站教程](https://mirrors.tuna.tsinghua.edu.cn/help/debian/#:~:text=%E4%B8%80%E8%88%AC%E6%83%85%E5%86%B5%E4%B8%8B%EF%BC%8C%E5%B0%86%20%2Fetc%2Fapt%2Fsources.list%20%E6%96%87%E4%BB%B6%E4%B8%AD,Debian%20%E9%BB%98%E8%AE%A4%E7%9A%84%E6%BA%90%E5%9C%B0%E5%9D%80%20http%3A%2F%2Fdeb.debian.org%2F%20%E6%9B%BF%E6%8D%A2%E4%B8%BA%E9%95%9C%E5%83%8F%E5%9C%B0%E5%9D%80%E5%8D%B3%E5%8F%AF%E3%80%82)
Debian Buster 以上版本默认支持 HTTPS 源。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Debian%2012%20-geng-gai-apt-yuan.html</guid><pubDate>Sat, 11 Oct 2025 05:52:07 +0000</pubDate></item><item><title>Debian下通过Wondershaper限制网速</title><link>https://doulifv.github.io/blog/post/Debian-xia-tong-guo-Wondershaper-xian-zhi-wang-su.html</link><description>安装git和make
```
apt install make git -y 
```
使用git拉取代码 、进入文件夹、使用make安装
```
git clone https://github.com/magnific0/wondershaper.git
cd wondershaper
make install
```
通过以下命令可以查看相关运行参数
```
wondershaper -h 
```
&gt;   -a 指定网络适配器
   -d 设置最大下载速率(单位为kbps)
   -u 设置最大上传速率(单位:kbps)
   -p 使用'/etc/systemd/wondershaper.conf'中的预设值
   -f 使用指定的预设文件
   -c 清除适配器的限制
   -s 显示适配器的当前状态

例如:限制网卡ens33的上传和下载为8000kbps每秒(8kbit/s=1kByte)
```
wondershaper -a ens33 -u 8000 -d 8000
```
设置完可以安装speedtest-cli测试一下限速效果
```
apt install speedtest-cli
speedtest #测试网速
```
卸载
```
make uninstall #卸载
reboot #重启
```
。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Debian-xia-tong-guo-Wondershaper-xian-zhi-wang-su.html</guid><pubDate>Sat, 11 Oct 2025 05:51:17 +0000</pubDate></item><item><title>Debian网络配置以及开启root远程登录</title><link>https://doulifv.github.io/blog/post/Debian-wang-luo-pei-zhi-yi-ji-kai-qi-root-yuan-cheng-deng-lu.html</link><description>查看需要配置的网卡名
```
ip address
```
编辑网卡配置文件/etc/network/interfaces
```
vi /etc/network/interfaces
```
添加以下字段为ens33这张网卡添加192.168.1.2这IP地址
```
auto ens33
iface ens33 inet static
address 192.168.1.2/24 #主机地址/掩码
gateway 192.168.1.1 #网关
```
添加DNS服务器地址
```
echo &gt;&gt; /etc/resolv.conf  nameserver 8.8.8.8
echo &gt;&gt; /etc/resolv.conf  nameserver 1.1.1.1
```
重启网卡、查看网卡IP配置情况
```
/etc/init.d/networking restart
ip address
```
允许root用户远程登录、重启ssh服务
```
echo PermitRootLogin yes &gt;&gt; /etc/ssh/sshd_config  
/etc/init.d/ssh restart
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Debian-wang-luo-pei-zhi-yi-ji-kai-qi-root-yuan-cheng-deng-lu.html</guid><pubDate>Sat, 11 Oct 2025 05:50:48 +0000</pubDate></item><item><title>Debian开启BBR加速</title><link>https://doulifv.github.io/blog/post/Debian-kai-qi-BBR-jia-su.html</link><description>Debian10-12默认编译了BBR拥塞控制算法可直接开启
命令如下
```
echo net.core.default_qdisc=fq &gt;&gt; /etc/sysctl.conf
echo net.ipv4.tcp_congestion_control=bbr &gt;&gt; /etc/sysctl.conf
sysctl -p
```
检查是否配置成功
```
sysctl net.ipv4.tcp_available_congestion_control
```
回显如下信息则表示成功开启BBR加速
&gt;net.ipv4.tcp_available_congestion_control = reno cubic bbr
。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Debian-kai-qi-BBR-jia-su.html</guid><pubDate>Sat, 11 Oct 2025 05:50:18 +0000</pubDate></item><item><title>Debian配置Fail2ban防止SSH账号密码被暴力破解</title><link>https://doulifv.github.io/blog/post/Debian-pei-zhi-Fail2ban-fang-zhi-SSH-zhang-hao-mi-ma-bei-bao-li-po-jie.html</link><description>安装fail2ban服务
```
apt install fail2ban
```
启动、设置开机自启动、查看状态
```
systemctl start fail2ban
systemctl enable fail2ban
systemctl status fail2ban
``` 
配置
[ ! ] 这里不建议直接修改/etc/fail2ban/jail.conf文件里面的参数
可以创建/etc/fail2ban/jail.local文件它会默认覆盖掉jail.conf文件里面的参数
``` 
vim /etc/fail2ban/jail.local
``` 
设置以下参数
```
[sshd]               
enabled = true       
port = 22 #默认端口号如不是请修改           
filter = sshd      
logpath = /var/log/fail2ban.log  #日志
maxretry = 3        #最大尝试次数
findtime = 8467200 #
bantime = 8467200 # 解用时间
backend = systemd
ignoreip = 127.0.0.1/8 192.168.0.1/16  #白名单网段/地址
```
重新启动Fail2ban服务
```
systemctl restart fail2ban
```
查看fail2ban ssh进程的禁用列表
 ```
fail2ban-client status sshd
```
解禁ban列表内IP
```
fail2ban-client set sshd unbanip [IP address]
```
&gt;例如：fail2ban-client set sshd unbanip 8.245.78.2
表示解禁8.245.78.2这个IP地址

卸载服务
```
systemctl stop fail2ban 
apt purge fail2ban
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Debian-pei-zhi-Fail2ban-fang-zhi-SSH-zhang-hao-mi-ma-bei-bao-li-po-jie.html</guid><pubDate>Sat, 11 Oct 2025 05:49:44 +0000</pubDate></item><item><title>RustDesk自托管</title><link>https://doulifv.github.io/blog/post/RustDesk-zi-tuo-guan.html</link><description>[https://rustdesk.com/docs/en/self-host/rustdesk-server-oss/install/](https://rustdesk.com/docs/en/self-host/rustdesk-server-oss/install/)
[https://www.cnblogs.com/ryanyangcs/p/18186163](https://www.cnblogs.com/ryanyangcs/p/18186163)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/RustDesk-zi-tuo-guan.html</guid><pubDate>Sat, 11 Oct 2025 02:11:18 +0000</pubDate></item><item><title>vmware linux挂载共享文件夹</title><link>https://doulifv.github.io/blog/post/vmware%20linux-gua-zai-gong-xiang-wen-jian-jia.html</link><description>安装依赖
```
apt install open-vm-tools open-vm-tools-desktop
```
查看共享文件夹[设置的共享名]
```
vmware-hgfsclient
```
设置共享文件夹
```
vmhgfs-fuse .host:/文件夹名/ /mnt/hgfs -o allow_other
```
```
cd /mnt/hgfs
```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/vmware%20linux-gua-zai-gong-xiang-wen-jian-jia.html</guid><pubDate>Sat, 11 Oct 2025 01:18:10 +0000</pubDate></item><item><title>VMware Tool</title><link>https://doulifv.github.io/blog/post/VMware%20Tool.html</link><description>

vmwaretools download link
https://packages.vmware.com/tools/releases/
blog link
title: 虚拟机安装VMware Tools时提示缺少KB2919355补丁
https://www.dinghui.org/vmware-tools-microsoft-kb2919355.html

查询VMware kb55798，是由于安装VMware Tools 10.3.x以上版本时，会检测环境是否具备Microsoft Visual C++ 2017 Redistributable先决条件。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/VMware%20Tool.html</guid><pubDate>Sat, 11 Oct 2025 01:12:50 +0000</pubDate></item><item><title>自用的VPS商家列表</title><link>https://doulifv.github.io/blog/post/zi-yong-de-VPS-shang-jia-lie-biao.html</link><description>[racknerd](https://racknerd.com/)
[massivegrid](https://www.massivegrid.com/)
[avenacloud](https://avenacloud.com/)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/zi-yong-de-VPS-shang-jia-lie-biao.html</guid><pubDate>Fri, 10 Oct 2025 12:40:11 +0000</pubDate></item><item><title>通过导出Windows注册表文件备份共享目录和权限</title><link>https://doulifv.github.io/blog/post/tong-guo-dao-chu-Windows-zhu-ce-biao-wen-jian-bei-fen-gong-xiang-mu-lu-he-quan-xian.html</link><description>export(admin).bat
```
@echo off
set rf=.\shareConfig.reg

reg export HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\lanmanserver\Shares %rf%
pause
```
import(admin).bat
```
@echo off
set rf=.\shareConfig.reg

reg import %rf%
net stop server
net start server
net share
pause
```
。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/tong-guo-dao-chu-Windows-zhu-ce-biao-wen-jian-bei-fen-gong-xiang-mu-lu-he-quan-xian.html</guid><pubDate>Fri, 10 Oct 2025 12:33:27 +0000</pubDate></item><item><title>windows 配置端口转发</title><link>https://doulifv.github.io/blog/post/windows%20-pei-zhi-duan-kou-zhuan-fa.html</link><description>#####以Windows7为例
#####显示当前正在使用的端口转发规则
```
netsh interface portproxy show all
```
#####添加端口转发
```
netsh interface portproxy add v4tov4 listenaddress=192.168.1.214 listenport=45622 connectaddress=192.168.6.128 connectport=45622

#源主机192.168.1.214:45622 转发到 192.168.6.128:45622
```
#####删除转发规则
```
netsh interface portproxy delete v4tov4 listenaddress=192.168.1.214 listenport=45622 
```
---
![image.png](https://upload-images.jianshu.io/upload_images/27021437-58e517f1e8f128c1.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240)

[netsh windows7 自带端口映射（转发）使用教程-CSDN](https://blog.csdn.net/u010953692/article/details/77045211)
[windows命令行下用netsh实现端口转发(端口映射)-CSDN](https://blog.csdn.net/dogquill/article/details/50143925)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/windows%20-pei-zhi-duan-kou-zhuan-fa.html</guid><pubDate>Fri, 10 Oct 2025 12:31:21 +0000</pubDate></item><item><title>Wireguard</title><link>https://doulifv.github.io/blog/post/Wireguard.html</link><description>### 一、安装 
安装需要的依赖
```
apt install wireguard
```
开启流量转发
```
echo 'net.ipv4.ip_forward = 1' &gt;&gt; /etc/sysctl.conf
sysctl -p
```
创建Wireguard &amp;&amp; 设置777任意读取执行权限
```
mkdir -p /etc/wireguard &amp;&amp; chmod 0777 /etc/wireguard
cd /etc/wireguard
```
生成公、私钥文件
```
wg genkey | tee server_privatekey | wg pubkey &gt; server_publickey
wg genkey | tee client_privatekey | wg pubkey &gt; client_publickey
```
生成的配置文件路径：/etc/wireguard/wg0.conf，命令如下：
```
echo '
[Interface]
PrivateKey = $(cat server_privatekey) # 填写本机的privatekey 内容
Address = 10.0.8.1/24
PostUp   = iptables -A FORWARD -i wg0 -j ACCEPT; iptables -A FORWARD -o wg0 -j ACCEPT; iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
PostDown = iptables -D FORWARD -i wg0 -j ACCEPT; iptables -D FORWARD -o wg0 -j ACCEPT; iptables -t nat -D POSTROUTING -o eth0 -j MASQUERADE
#注意修改对应网卡名称
ListenPort = 50814 # 注意该端口是UDP端口
DNS = 8.8.8.8
MTU = 1420
[Peer]
PublicKey =  $(cat client_publickey)  # 填写对端的publickey 内容
AllowedIPs = 10.0.8.10/24 ' &gt; wg0.conf
```
还需要设置开机启动：
```
systemctl enable wg-quick@wg0
```
客户端配置文件生成
```echo '
[Interface]
  PrivateKey = $(cat client_privatekey)  # 填写本机的privatekey 内容
  Address = 10.0.8.10/24
  DNS = 8.8.8.8
  MTU = 1420

[Peer]
  PublicKey = $(cat server_publickey)  # 填写对端的publickey 内容
  Endpoint = server公网的IP:50814
  AllowedIPs = 0.0.0.0/0, ::0/0
  PersistentKeepalive = 25 ' &gt; client.conf
```
### 服务的启动 | 停止

#### 服务端启动

启动或停止**wireguard**服务端的命令如下：
 启动WireGuard
```
wg-quick up wg0
```
停止WireGuard
```
wg-quick down wg0
```
查看wireguard服务端运行状态命令：
```
wg
```
#### 客户端启动
启动或停止wireguard客户端的命令如下：
启动WireGuard
```
wg-quick up client
```
停止WireGuard
```
wg-quick down client
```
---
[Err-1](https://askubuntu.com/questions/1456439/wireguard-error-message-resolvconf-command-not-found)
/usr/bin/wg-quick: line 32: resolvconf: command not found
```
apt install openresolv
```
[Err-2]
/usr/bin/wg-quick: line 295: iptables: command not found
```
apt install iptables
```
---
参考教程
[Installation - WireGuard](https://www.wireguard.com/install/)
[组网神器WireGuard安装与配置教程（超详细）_wirguard-CSDN博客](https://blog.csdn.net/qq_20042935/article/details/127089626)
[/usr/bin/wg-quick：第 31 行：resolvconf：找不到命令 [WireGuard | Debian] - 超级用户 --- /usr/bin/wg-quick: line 31: resolvconf: command not found [WireGuard | Debian] - Super User](https://superuser.com/questions/1500691/usr-bin-wg-quick-line-31-resolvconf-command-not-found-wireguard-debian)
[如何在Ubuntu 20.04安装WireGuard VPN | myfreax](https://www.myfreax.com/how-to-set-up-wireguard-vpn-on-ubuntu-20-04/)


。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Wireguard.html</guid><pubDate>Fri, 10 Oct 2025 12:24:51 +0000</pubDate></item><item><title>windows7关闭Google Chrome烦人的更新提示</title><link>https://doulifv.github.io/blog/post/windows7-guan-bi-Google%20Chrome-fan-ren-de-geng-xin-ti-shi.html</link><description>使用TXT文本编辑器
新建注册表文件，更给TXT后缀为 **.reg** 
closeChromeUpdateWarning.reg
```
Windows Registry Editor Version 5.00

[HKEY_CURRENT_USER\Software\Policies\Google\Chrome]
'SuppressUnsupportedOSWarning'=dword:00000001
```
双击添加到注册表即可

---
[如何关闭“若要接收后续 google chrome 更新,您需使用 windows 10 或更高版本”](https://www.jianshu.com/p/d1001a98fb77)
  。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/windows7-guan-bi-Google%20Chrome-fan-ren-de-geng-xin-ti-shi.html</guid><pubDate>Fri, 10 Oct 2025 12:20:04 +0000</pubDate></item><item><title>debian下以光盘为apt源安装软件包</title><link>https://doulifv.github.io/blog/post/debian-xia-yi-guang-pan-wei-apt-yuan-an-zhuang-ruan-jian-bao.html</link><description>对于一些停止维护的debian版本网络软件包资源大多停止维护，此时可以通过挂载ISO安装光盘实现本地软件包安装
```
apt-cdrom add
```
系统会提示将你要添加为源的光盘放入光驱
然后enter继续，就自动添加好了
&gt;root@214:~# **apt-cdrom add**
Using CD-ROM mount point /media/cdrom/
Unmounting CD-ROM...
Waiting for disc...
**Please insert a Disc in the drive and press [Enter]**
Mounting CD-ROM...
Identifying... [3499991a1c974d34b1f07cea4df324ac-2]
Scanning disc for index files...
Found 2 package indexes, 0 source indexes, 2 translation indexes and 0 signatures
This disc is called: 
'Debian GNU/Linux 9.13.0 _Stretch_ - Official amd64 DVD Binary-1 20200718-11:07'
Reading Package Indexes... Done
Reading Translation Indexes... Done
Writing new source list
Source list entries for this disc are:
***deb cdrom:[Debian GNU/Linux 9.13.0 _Stretch_ - Official amd64 DVD Binary-1 20200718-11:07]/ stretch contrib main***
Unmounting CD-ROM...
Repeat this process for the rest of the CDs in your set.
---
[捷杰耶夫csdn-](https://blog.csdn.net/whw8007 '捷杰耶夫')-[debian下以光盘为apt源安装软件包](https://blog.csdn.net/whw8007/article/details/8833083)。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/debian-xia-yi-guang-pan-wei-apt-yuan-an-zhuang-ruan-jian-bao.html</guid><pubDate>Fri, 10 Oct 2025 12:17:10 +0000</pubDate></item><item><title>Hello world</title><link>https://doulifv.github.io/blog/post/Hello%20world.html</link><description>```Hello world```。</description><guid isPermaLink="true">https://doulifv.github.io/blog/post/Hello%20world.html</guid><pubDate>Fri, 10 Oct 2025 11:54:37 +0000</pubDate></item><item><title>about</title><link>https://doulifv.github.io/blog/about.html</link><description>Writing...Please wait.。</description><guid isPermaLink="true">https://doulifv.github.io/blog/about.html</guid><pubDate>Sat, 11 Oct 2025 08:01:40 +0000</pubDate></item><item><title>link</title><link>https://doulifv.github.io/blog/link.html</link><description>纯个人收藏
| LINK | NOTE |
| ----------- | ----------- |
|[Zapro Notice](https://tmioe.com/) | 🔥软件 电影  纪录片 [zaproshare](https://t.me/zaproshare)|
|[懒得勤快](https://masuit.org/)| 官方网站：https://masuit.org 备用地址：https://ldqk.xyz ，官方讨论组：[masuit_org](https://t.me/masuit_org)|
|[TGweba](https://t.me/TGweba)|擅长搜索的高木同学 关注后请先阅读频道置顶 Please read the channel first after following my channel.|
|[Nite的小窝](https://www.nite07.com)| 博客: https://www.nite07.com  软件站: https://software.nite07.com  游戏站: https://game.nite07.com  Vaultwarden: https://passwd.nite07.com [TGChannel](https://t.me/nitechan) |
|[胖虎の收藏夹](https://t.me/gitbig)|乜都有|
|puresys 纯净系统  软件下载 | https://www.puresys.net/|
|549.tv|http://549.tv|
---
| NOTE| LINK |
| ----------- | ----------- |
| 奇客资讯网 | https://www.solidot.org/ |
|lowendtalk|https://lowendtalk.com|。</description><guid isPermaLink="true">https://doulifv.github.io/blog/link.html</guid><pubDate>Sat, 11 Oct 2025 12:15:49 +0000</pubDate></item></channel></rss>